# Status

- Current phase: the owner-provided 20-club PNG badge replacement is live and complete. The tested feature is merged, the stable alias serves the new production deployment, all 20 production paths use PNGs, and final mobile evidence is refreshed.
- Product: user-facing identity is Dranx Prediction League; mobile-first prediction, private receipt, champion-first leaderboard/comparison, owner settings/submission deletion, manual standings, authenticated source-neutral imports, and explicit final-table controls remain the product scope.
- Vercel: project `vdoshi96s-projects/pl-predictions`; production alias [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app).
- Feature release deployment: `dpl_2z72V3tGKDssEAggjqMEH4PQQsYN` is Ready with target `production` at immutable URL [https://pl-predictions-6hs8vokup-vdoshi96s-projects.vercel.app](https://pl-predictions-6hs8vokup-vdoshi96s-projects.vercel.app). Its metadata identifies GitHub `main` merge `1c8a20b`; the stable public alias points to this deployment and serves the home page plus raw PNG assets with 200 responses. The final evidence-only closeout deployment is intentionally not pinned because it changes no runtime application behavior.
- Access boundary: the stable public alias returns 200, while the retained preview continues to redirect to Vercel SSO with 302. Preview protection remains enabled.
- Database: Vercel Marketplace Neon resource `neon-coffee-queen`; migrations `0000`, `0001`, and `0002` are applied to production. Migration `0002` persists each season's opening kickoff; the 2026/27 row is `2026-08-21T19:00:00.000Z` with a null optional earlier deadline, unlocked and unrevealed. The deploy-first transition completed: exactly 20 verified clubs remain present, all 20 asset paths exactly match the canonical PNG fixture, and no SVG path remains. The before/after non-path team metadata digest stayed `dc907bd0e26186a76d62210381ee593f44225f7c0557f8cede6d43c7dfae7910`.
- Standings: no runtime football API, scraper, or Vercel Cron. Manual admin entry and the bearer-authenticated canonical importer preserve the last good snapshot. FotMob automation remains prohibited absent written permission.
- Submission ceiling: official sources confirm Arsenal v Coventry City is the first match at `2026-08-21T19:00:00.000Z`. That season-scoped persisted instant is a non-extendable database-clock cutoff; an owner deadline can close earlier but cannot extend past kickoff. The atomic insert samples PostgreSQL's wall clock only after its season-row lock. Fixtures remain subject to change, so a reschedule requires reviewed code and forward data migration before the cutoff.
- Leaderboard: before kickoff, every participant shows 0 points and only the predicted champion's local club mark/name. Prediction UUIDs and positions 2–20 remain private. After meaningful post-kickoff standings exist, totals are derived on read and the champion card shows on track in 1st or off track at its current actual ordinal position.
- Finalization: all 20 clubs must show 38 played games; finalization and undo each compare-and-swap the exact active/final snapshot. Imports use a monotonic accepted-through watermark so duplicate observations cannot permit a later standings regression.
- Brand and assets: the Premier-League-inspired palette uses official purple `#37003c` with cyan `#05f0ff`, green `#00ff87`, and pink `#ff2882` accents and an original Dranx mark. The project owner's 20 transparent local PNG club badges are the canonical team assets; `TeamMark` uses contain sizing and a labelled initials fallback. Original SVG monograms remain rollback-only for the first PNG release. The official Premier League logo and the supplied league logo/lion/ball/composite extras are not used.
- Local verification: documentation parity, formatting, ESLint, strict TypeScript, 115 default tests with 10 guarded skips, all 10 isolated Neon integration tests, the Webpack production build, and 8 focused browser journeys with 22 intentional project-routing skips passed. Browser coverage now requires all 20 club-mark images to be loaded PNGs. A separate headed Chromium inspection confirmed all 20 optimized image requests returned 200, the varied badge shapes remained legible, the console had zero warnings/errors, and the narrow viewport had no horizontal overflow.
- Production verification: 5 of 5 read-only smoke projects passed against the stable alias across desktop Chromium, mobile Chromium at 390 pixels, exact 320/430-pixel reflow, and mobile WebKit. Every prediction and review-dialog club mark completed with a nonzero natural width from a PNG source; narrow browser-cancelled Next Image candidates are ignored only after rendered-image success is required. An independent headed run found 20 loaded PNG marks, 20 optimized 200 responses, no initials fallbacks, no console warnings/errors, and no horizontal overflow. The exact deployment's newest 500 log records across the inspected 30-minute window were all HTTP 200 with zero warnings, errors, 4xx, or 5xx responses.
- Dependency note: the full audit reports 4 moderate development-only legacy `esbuild` paths through `drizzle-kit`, with no high or critical findings. The breaking force-downgrade suggestion was not applied.
- GitHub: [PR #5](https://github.com/vdoshi96/Pl-predictions/pull/5) merged the tested badge feature into canonical `main` at `1c8a20bc0a689f39d5cb392753d3582aa616bdbc`. The final evidence-only closeout merge is intentionally not pinned in this end-state record.
- Production data: the explicitly approved idempotent seed changed the 20 club asset paths from SVG to their exact PNG fixture values. The non-path team metadata digest was unchanged, and prediction, standings-snapshot, and standings-item counts each remained zero. No participant or standings write smoke ran.
- Documentation: canonical Markdown records the owner-provided badge handoff, implementation, deploy-first transition, exact production proof, and newest inspected screenshots; all HTML peers are regenerated from those sources.
- Repository closeout: local and remote `main` are synchronized at the final closeout merge, completed badge branches/worktree are removed, and only the primary worktree remains.
