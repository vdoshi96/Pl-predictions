interface:
  display_name: "Update results"
  short_description: "Publish reviewed table, spotlight, and Win Streak results"
  default_prompt: "Use $update-results to check official Win Streak fixtures and refresh reviewed live-season standings, spotlight results, and completed Win Streak outcomes through authenticated admin workflows."

policy:
  allow_implicit_invocation: false
