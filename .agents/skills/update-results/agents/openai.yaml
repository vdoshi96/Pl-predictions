interface:
  display_name: "Update results"
  short_description: "Sync fixtures and publish reviewed live results"
  default_prompt: "Use $update-results to verify or safely sync official Win Streak fixtures, then refresh reviewed live-season standings, spotlight results, and completed Win Streak outcomes through authenticated admin workflows."

policy:
  allow_implicit_invocation: false
