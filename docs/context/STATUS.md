# Status

- Current phase: implementation and pre-merge verification for the August 13 roster and portrait refresh are complete in the separate `roster-2026-08-13-player-update` worktree. The earlier how-to/countdown/complete-test-entry release remains the production application until the reviewed branch is merged, deployed, and seeded.
- Feature state: local branch `roster/2026-08-13-player-update` began this pass at `7d1996ca5e11dd279fd36cc33d4f7269befd613f`, one direct commit ahead of live `main`. It incorrectly tracked `origin/main`; no remote feature branch, pull request, or feature checks existed at the read-only audit. The worktree remains active with the completed, locally verified remediation pending a fresh live-ancestry check and publication.
- Product: Dranx Prediction League uses one immutable three-stage entry: order all 20 clubs and enter a display name, choose seven spotlight predictions, then review and submit. Receipt privacy, derived leaderboard/comparison, owner settings and exact submission deletion, manual standings, authenticated source-neutral import, and final-table controls are unchanged.
- Spotlight categories: top scorer, top assister, most clean sheets, underdog team, overrated team, underdog player, and overrated player. Player categories use searchable keyboard-accessible selectors plus Other-player text input; most clean sheets and both team-opinion categories use clubs and the existing crests.
- Candidate player catalogue: the owner-selected August 13 handoff and local fixture contain 582 players across the 20 clubs and 582 portrait paths. Relative to August 8, the active fixture has 12 additions, 17 removals, four intra-league moves, seven restored portraits, and ten portrait replacements. The workbook, raw/final JSON, source/public images, and tracked fixture reconcile exactly; this remains internal verification and does not claim independent official-club or Premier League roster verification. Other remains available, and `PlayerMark` still falls back for an absent path or failed image.
- Compatibility decision: use the exact refreshed portrait set with 17 obsolete paths removed and four club-move path renames. Do not retain legacy compatibility copies. Historical database player rows remain because the seed deactivates absent fixture players instead of deleting them.
- Vercel: project `vdoshi96s-projects/pl-predictions`; stable production alias [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app). The last verified immutable application deployment remains `dpl_2L4cprnsWShhesujY7sTLKffywhg` at [https://pl-predictions-9h7r762li-vdoshi96s-projects.vercel.app](https://pl-predictions-9h7r762li-vdoshi96s-projects.vercel.app).
- GitHub baseline: live `main` is `38aa37ad42cb684aacb2120d23aefa3d6f145d4d`, the PR #14 documentation closeout. The roster commit is not present on GitHub. Main has a successful Vercel status, no branch-protection rule or ruleset, and all three merge methods are enabled; the release will self-impose the approved green-check/no-bypass PR gate.
- Production database: Vercel Marketplace Neon resource `neon-coffee-queen`; migrations `0000` through `0005` are applied. Production remains on the August 8 seed baseline of 587 total/active players and 580 portrait paths. No August 13 production seed has run.
- Planned production transition: merge and deploy the application first, wait for the exact production deployment to become Ready, then prove the configured Neon recovery window and a usable just-before-seed recovery point. Only after that stop gate passes may the approved idempotent production seed run exactly once, followed by read-only database/browser/image verification. Integration and browser tests remain isolated from production.
- Atomicity and deletion: a guarded prediction write accepts one parent only with all 20 ordered team rows and exactly seven spotlight rows, otherwise none persist. Authenticated exact deletion cascades through both child sets, preserves its deletion audit, and allows the normalized display name to submit again.
- Leaderboard and privacy: the main leaderboard uses table points only and has a 100-point maximum. `/spotlight` tracks separate fun accuracy. Before reveal, identifiers, positions 2–20, spotlight picks, and hidden accuracy ordering remain private. Only complete persisted entries contribute to the bracket count and eventual rankings.
- Scoring: the predicted champion remains table position 1 without a separate bonus. With `N` active, nondeleted brackets, spotlight accuracy is `max(0, N + 1 - outcome rank)` and sums only resolved categories. Equal overall scores share a competition rank. Team underdog uses `average predicted position - actual position`; overrated uses the inverse.
- Pending result inputs: a future owner-run Codex automation may acquire top scorer, top assister, most clean sheets, underdog player, and overrated player data offline, review it, and enter it through an approved path. The roster snapshot is selector input, not an outcome feed, and custom Other-player names still require reconciliation.
- Runtime boundary: the deployed application has no runtime football API, scraper, image hotlink, or Vercel Cron. Manual administrator entry and the authenticated canonical importer preserve the last good complete 20-team standings permutation.
- Submission and security: the persisted non-extendable opening instant remains `2026-08-21T19:00:00.000Z`; the database clock remains authoritative. Administrator PBKDF2 login, signed cookies, same-origin mutation checks, WAF protection, receipt hashing, and the participant data-minimization boundary are unchanged.
- Local verification: the uninterrupted `CI=1 npm run check` passed 14-document parity, the 582-player/portrait validator, formatting, ESLint, strict TypeScript, 205 unit/component tests with 11 guarded integration skips, all 11 isolated Neon integration tests, the Webpack production build, five pre-kickoff browser journeys, and three post-kickoff journeys. The separate focused set passed 77 tests, and the 33-tile additions/restorations/replacements/moves contact sheet passed visual inspection.
- Production verification: the prior deployment's five-project read-only smoke and seven retained mobile artifacts remain historical evidence only. They do not verify the August 13 roster or portraits.
- Release placeholders: feature PR `TBD after push`; merge SHA `TBD after merge`; Ready production deployment and stable-alias proof `TBD after deploy`; production seed/database/browser/image result `TBD after approved seed`; final documentation-closeout PR/SHA `TBD if required`.
- Documentation and repository closeout: canonical Markdown now records the completed local evidence and will be regenerated before publication. The primary checkout and roster worktree both remain; the August 8 and August 13 handoffs remain private, untracked, and exactly ignored. The retained August 13 copies are byte-identical at 613 files and full manifest SHA-256 `f625470979c156e233300ec42924eb858255d902a456af1283a089ae41169630`. Worktree/branch cleanup is not allowed until merge, production verification, evidence closeout, and retained-handoff hash verification succeed.
