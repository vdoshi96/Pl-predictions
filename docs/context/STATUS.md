# Status

- Current phase: implementation, production deployment, anonymous production verification, bounded write cleanup, documentation parity, and GitHub publication are complete.
- Product: mobile-first prediction, private receipt, leaderboard/comparison, owner settings/submission deletion, manual standings, authenticated source-neutral imports, and explicit final-table controls implemented.
- Vercel: project `vdoshi96s-projects/pl-predictions`; production alias [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app).
- Smoke-verified production runtime: deployment `dpl_JCfjcxvtVVfbsU55mmTppBK2FB9G` is Ready at immutable URL [https://pl-predictions-65aqenfnq-vdoshi96s-projects.vercel.app](https://pl-predictions-65aqenfnq-vdoshi96s-projects.vercel.app). The earlier edge 404 was traced to a null Vercel framework preset and resolved by setting the project preset to `nextjs` before this deployment. The stable production alias remains the public release URL even when a later documentation-only Git deployment advances its target.
- Access boundary: after explicit owner approval, Vercel Authentication was changed to `preview`. Anonymous production health and application requests return 200, while the retained preview URL continues to redirect to Vercel SSO with 302.
- Database: Vercel Marketplace Neon resource `neon-coffee-queen`; migrations `0000` and `0001` applied to production, idempotent seed rerun, and exactly 20 verified 2026/27 clubs present. Automated DB/browser suites use isolated database `pl_predictions_test` through a fail-closed wrapper.
- Standings: no runtime football API, scraper, or Vercel Cron. Manual admin entry and the bearer-authenticated canonical importer preserve the last good snapshot. FotMob automation remains prohibited absent written permission.
- Finalization: all 20 clubs must show 38 played games; finalization and undo each compare-and-swap the exact active/final snapshot. Imports use a monotonic accepted-through watermark so duplicate observations cannot permit a later standings regression.
- Assets: local text monograms are the launch fallback because official crest redistribution rights are not cleared.
- Verification: 95 default tests passed with 9 opt-in database skips; all 9 isolated Neon integration tests passed; the Webpack production build passed; 5 focused local browser journeys passed with 20 intentional routing skips. The final anonymous production smoke passed 5 of 5 projects across desktop Chromium, 320/390/430-pixel Chromium, and iPhone WebKit with mouse/touch/keyboard reorder, no horizontal overflow, healthy responses, and zero console, page, unexpected same-origin request, or HTTP response errors. The bounded production write smoke passed 1 of 1.
- Dependency note: the full audit reports 4 moderate development-only legacy `esbuild` paths through `drizzle-kit`, with no high or critical findings. The breaking force-downgrade suggestion was not applied.
- GitHub: public owner-supplied repository [vdoshi96/Pl-predictions](https://github.com/vdoshi96/Pl-predictions); the final evidence release is published on canonical `main`, with local and remote `main` synchronized and no completed worktree retained.
- Production cleanup: the bounded smoke created one exact QA prediction, proved receipt privacy and the hidden pre-reveal roster, then deleted that exact entry through the administrator flow. Production now has zero predictions, prediction items, and prediction audits; the season remains open, unrevealed, and unlocked with null active/final standings pointers.
- Documentation: canonical Markdown now records the public-access boundary, final production evidence, and newest labeled mobile screenshots; deterministic same-content HTML peers are generated and checked.
- Remaining closeout: none.
