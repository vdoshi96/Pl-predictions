# Status

- Pending Win Streak release (2026-09-01): branch `vishal/win-streak-name-resume` lets a participant enter the same normalized display name after cookie loss. The server rotates the receipt for the existing profile while preserving its identity, joined round, history, and immutable picks. Migration `0011_win_streak_name_resume.sql` changes only the profile trigger's receipt-rotation allowance.
- Win Streak resumption verification: 423 unit and component tests, the focused isolated Neon receipt-rotation integration test, Prettier, ESLint, strict TypeScript, documentation parity for 18 pairs, and the Next.js 16.3 Webpack production build pass. The Playwright journey is updated, but the dated aggregate browser fixture still leaves Matchweek 2 unresolved after its real PostgreSQL deadline. Migration `0011` is not applied to production, and the branch is not merged or deployed.
- Current production data (2026-08-31): the authenticated owner workflow published the 20-club FotMob table and sealed provisional snapshots for goals, assists, clean sheets, and player ratings. The season has `N = 14` brackets. Active snapshot IDs are goals `fc93b34a-77d6-41ae-bc35-fcbd95e600a0`, assists `114b6a29-1b8c-46a7-8714-c9b9a1b1cfc7`, clean sheets `b1e038a1-cf6b-4ac8-bfe1-7c241b81e388`, and ratings `e364b977-b15e-4ef0-ba54-790e99af1b51`. Every active snapshot is the sealed working snapshot, and every final pointer remains `NULL`.
- Rating availability: 18 of 22 distinct picked opinion players have reviewed ratings. Amad Diallo, Brian Madjo, Carlos Baleba, and Martin Dúbravka remain absent from the rating snapshot and display as N/A without a rank or accuracy points.
- Public results: [the production site](https://pl-predictions-2026.vercel.app) renders Adi Ashok first on 39 table points, Angad second on 38, and Aravind third on 36. Keshav leads Spotlight with 82 accuracy points. All seven category boards are live; the four unrated picked players remain N/A.
- Win Streak: all 10 Matchweek 2 results are reviewed and stored atomically with no voids. Rohan Tejaswi and Vishal Doshi both picked winning Manchester United, share first with current and best streaks of one, and have no Matchweek 3 pick. Matchweek 3 is open.
- Fixture and routing verification: the official check retained 370 fixtures across 37 matchweeks with the August 28 source date and no drift. No fixture release or targeted seed ran. Before this documentation closeout, `pl-predictions-2026.vercel.app` resolved to Ready production deployment `dpl_HLqSB2C8vJpyMUpEeajMhY8YBabd`; anonymous desktop and 390-pixel checks returned `200` without console warnings or failed application requests.
- Administrator follow-up: Next.js 16.3 rejects the current `/admin/win-streak` server-action module because the `"use server"` file exports a client initial-state object. The August 31 update used a temporary local-only module split to run the same authenticated action against the verified live database, then restored the tracked source. Fix the module export before the next Win Streak result run.

- Current release (2026-08-28): [PR #46](https://github.com/vdoshi96/Pl-predictions/pull/46) merged reviewed rating subsets and public N/A presentation as `e852d717ed6af12cd54916e5e0ec5475abce3f98`. A player omitted from the reviewed rating snapshot remains unavailable and contributes no points; no zero or sentinel fact is stored. Exact production deployment `dpl_5bVFdeCYjckhPwPWe2gSHYMo6YMi` is Ready at [its immutable URL](https://pl-predictions-hdaopixm0-vdoshi96s-projects.vercel.app) and owns the automatic main/project aliases.
- Rating publication: production snapshot `3bd5c440-0add-4de2-800c-e5757e21efea` is sealed, active, working, provisional, sourced to FotMob, and records `N = 14`. All 15 stored picked-player ratings match the reviewed source. Seven of 22 picked players have no reviewed rating and display as N/A: Alejandro Garnacho, Amad Diallo, Brian Madjo, Carlos Baleba, Liam Delap, Martin Dúbravka, and Omar Marmoush. The exact deployment renders both opinion-player categories live, the seven N/A rows without ranks or points, Keshav first overall with 90 points, and entry availability from six to seven results.
- Verification boundary: documentation parity, the player-catalogue validator, formatting, ESLint, strict TypeScript, 422 unit/component tests, the exact four-test Spotlight integration suite, and the Webpack production build pass. The aggregate integration/browser gate also exposed stale Win Streak tests that assume Matchweek 2 remains open after its real PostgreSQL deadline; two database cases and related profile-creation journeys fail for that unrelated date condition. No schema migration, fixture change, direct result write, finalization, LOCK, or REVEAL ran.
- Vercel routing at the August 28 closeout: `pl-predictions-2026.vercel.app` was still a deployment-level alias pinned to August 24 deployment `dpl_5znWdTiecQjPMyyS56qBVc4YiAcv`. The owner approved converting it to the project production domain on August 29; the current production section records that completed correction.

- Current release (2026-08-24): [PR #43](https://github.com/vdoshi96/Pl-predictions/pull/43) merged picked-subject opinion rankings as `0abe166dc043820dc5201c9d6219b52a3fb39742`. Each opinion category now ranks only its distinct picked subjects. Underdog and overrated players use separate filtered views of the active shared rating snapshot. Team expectation indexes still use every complete bracket before their candidate lists are filtered to the relevant team picks. Existing public ranks recalculate at read time without rewriting or republishing sealed result snapshots.
- Picked-subject local verification: documentation parity for 18 pairs, the 580-player/578-portrait validator, formatting, ESLint, strict TypeScript, 419 unit/component tests with 33 guarded skips, all 31 isolated Neon integration tests, and the Webpack production build pass. A focused optimized-browser matrix passes `/spotlight`, revealed entry details, and `/admin/results` at desktop, 320 pixels, and 430 pixels with category-separated rating pools and no page overflow.
- Picked-subject production verification: Ready deployment `dpl_5znWdTiecQjPMyyS56qBVc4YiAcv` owns the stable production alias. The guarded five-browser read-only smoke passes. A separate raw-SQL recomputation matches 112 rendered opinion assignments across desktop and 390-pixel mobile. Read-only database capture at WAL `0/FA28990` finds zero rows created or updated after deployment, unchanged 14/280/98 prediction aggregates, exact standings and result pointers, 32 result snapshots with 1,883 immutable facts, zero aliases, and 75 existing audits. Bounded error, fatal, and HTTP-500 log queries are empty. No schema, migration, seed, result transition, or production database mutation ran.

- Current UI/UX release (2026-08-23): [PR #41](https://github.com/vdoshi96/Pl-predictions/pull/41) merged as `a09798709e461fff511ea4712a3b1a8bb9649f86`. A complete interaction-first audit covers all 13 UI routes with 117 timestamped manifest entries across desktop, mobile, anonymous, receipt-holder, and administrator states. It fixes the Rules carousel's serious accessibility failure and 768-pixel document overflow, adds keyboard-visible containment, and clarifies searchable-dropdown, prediction-stage, confirmation-dialog, overlay, and success state changes with reduced-motion-safe transitions. The canonical evidence is `docs/ux-audit-2026-08-23.md` and its generated HTML peer.
- UI/UX local verification: the uninterrupted `CI=1 npm run check` passes parity for 18 documentation pairs, the 580-player/578-portrait validator, formatting, ESLint, strict TypeScript, 414 unit/component tests with 31 guarded skips, all 29 isolated Neon integration tests, the Webpack production build, 24 pre-kickoff browser journeys with 26 intentional routing skips, and three post-kickoff journeys with two intentional skips. Separate route, responsive, axe, console, network, offline, reduced-motion, and state-transition probes also pass against the optimized build.
- UI/UX production verification: Ready deployment `dpl_2c3dpxwrtkPhQNbqRRyga8ti3QDb` at `https://pl-predictions-d35jddef5-vdoshi96s-projects.vercel.app` reports the exact merge through GitHub's production deployment record. Its guarded five-browser read-only smoke passed before the stable alias was assigned; the same matrix passed again at `https://pl-predictions-2026.vercel.app`. A final 12-route desktop/mobile audit found zero axe violations, document overflow, console warnings/errors, or HTTP failures, and bounded error, fatal, and HTTP-500 log queries were empty.
- UI/UX verification boundary: production supplied the anonymous read-only baseline. Participant writes, receipt checks, and administrator previews ran only against the isolated test database; no production participant, administrator, lock, reveal, standings, result, fixture, migration, seed, or database mutation was part of the audit. The one open medium finding is privacy-sensitive rediscovery of a participant's own confirmation after leaving the success screen.

- Current release (2026-08-23): Win Streak [PR #39](https://github.com/vdoshi96/Pl-predictions/pull/39) merged as `19bf11b4df43f8c3410f704fb278d1d4bdd845b5`. `/win-streak` is live for Matchweeks 2–38 with 37 official rounds and 370 seeded fixtures. The leaderboard is public without entering a display name and includes personal best, current streak, and the current-matchweek pick. Playing uses a receipt-bound display name rather than a login. `/admin/win-streak` provides the reviewed all-ten-results transition.
- Win Streak local verification: the uninterrupted `CI=1 npm run check` passes parity for 17 documentation pairs, the player validator, formatting, ESLint, strict TypeScript, 413 unit/component tests with 31 guarded skips, all 29 isolated Neon integration tests, the Webpack production build, 24 pre-kickoff browser journeys with 26 intentional routing skips, and three post-kickoff journeys with two intentional skips. The separate online official-fixture check passes with no drift. The focused 16-journey Win Streak matrix spans desktop, 320-, 390-, and 430-pixel Chromium plus mobile WebKit and covers public access, immutable picks, opposing picks, persistence, keyboard operation, light/dark presentation, clean browser consoles, and no horizontal overflow. Final read-back found zero test profiles, picks, resolved rounds, rate limits, or post-kickoff residue.
- Win Streak production verification: repeatable-read preflight, exact-hash migration `0010`, targeted fixture seed, and postchecks pass with unchanged existing aggregates. Ready deployment `dpl_BTncZC77GmA3EXtaUjAKvfWZvRzP` owns the stable production alias. The guarded five-browser anonymous smoke passes `/win-streak` with no mutations, console errors, or overflow; final database state has zero Win Streak profiles, picks, results, resolved rounds, or rate-limit rows, and bounded deployment error/fatal/HTTP-5xx queries are empty. Matchweek 2 starts August 28, so no result backfill is due. The recurring update-results workflow now checks official fixture drift and fails closed outside safe future kickoff-only changes.

- Current release (2026-08-22): [PR #33](https://github.com/vdoshi96/Pl-predictions/pull/33) merged directional consensus deltas as `0cf3caf9602bbbf10b2cf233c6676baf1005e9cc`. Every nonzero delta at the displayed one-decimal precision uses `▲` or `▼`; only displayed `0.0` remains neutral. Vercel deployment `dpl_8L2gqq4KjFiByeRrcVkXsWGFWjQQ` is Ready at [its immutable URL](https://pl-predictions-plwwrm1l9-vdoshi96s-projects.vercel.app), and deployment-ID read-back proves the stable [production alias](https://pl-predictions-2026.vercel.app) resolves to it.
- Current release verification: formatting, ESLint, strict TypeScript, 347 enabled unit/component tests, all 22 isolated Neon integration tests, the Webpack production build, and the optimized post-kickoff production-server matrix pass. The bounded five-browser production smoke passes. A live independent recomputation matches all 20 rows; Arsenal renders `▲ 0.1`, Brentford renders `▲ 0.3`, the 390-pixel table has no horizontal overflow, the browser console is clean, and deployment error and HTTP 5xx queries return no records.
- Current release boundary: this display-only release ran no migration, seed, administrator mutation, or production database write. Production checks used anonymous read-only requests.
- Previous August 22 release: [PR #31](https://github.com/vdoshi96/Pl-predictions/pull/31) merged the dark-mode, leaderboard-detail, and dual-zone time changes as `eb1bc82a50f850ff5965c80d3d933e36e8c3f6b5`. Vercel deployment `dpl_252wwia4AqFJevuPhTiWZrwwR3a7` became Ready at [its immutable URL](https://pl-predictions-75wicw45f-vdoshi96s-projects.vercel.app), and the stable production alias was explicitly assigned and verified before the directional-delta release replaced it.
- Product change: the site follows the operating-system light or dark preference through semantic color tokens and matching browser theme metadata. Leaderboard score tracks retain a visible border, and the top-three cards keep rank pills and crown/medal markers outside clipped card content while preserving first-to-third document order. Site date-time labels show Chicago local time followed by UTC; the administrator time-zone comparison keeps its optional third-zone view.
- Release verification: formatting, ESLint, strict TypeScript, 346 enabled unit/component tests, all 22 isolated Neon integration tests, and the Webpack production build pass. The optimized local production server passed seven routed pre-kickoff journeys, the focused mobile WebKit rerun, and three routed post-kickoff journeys. The stable production alias passed the bounded five-browser anonymous smoke. Live desktop and 390-pixel checks verified both color schemes, dual-zone text, podium ordering, the bordered progress track, no horizontal overflow, zero console errors, and no deployment error or HTTP 5xx log records.
- Release boundary: this code-only release ran no migration, seed, administrator mutation, or production database write. The production smoke used only read-only same-origin methods.
- Review behavior change: applying migration `0008` creates the persistent administrator session registry, so existing signed administrator cookies will require one fresh login after release. No participant prediction, standings fact, result fact, pointer, lock, reveal, or roster row is changed by migrations `0008`/`0009`.

- Current product change (2026-08-21): [PR #26](https://github.com/vdoshi96/Pl-predictions/pull/26) merged the approved season-glance display as `ee325557b268510bafa9c7b54aa3b46cba509da4`. `/` retains the three-stage prediction form before reveal and becomes the live Premier League table versus revealed consensus afterward. `/leaderboard` uses a top-three podium and dense shared-rank table with previous-snapshot movement. `/spotlight` defaults to seven category boards and retains the entry list and desktop matrix through explicit view links. Both Vercel checks passed. No schema, dependency, environment, production-data, or production-deployment change was part of this iteration.
- Previous repository change (2026-08-21): [PR #24](https://github.com/vdoshi96/Pl-predictions/pull/24) merged the administrator paste-entry implementation as `0a5ad854fe522993ff668ce06ffa94a4c9c0fed8`. Local unit, isolated Neon, production-build, production-server browser, desktop, 375-pixel, and 320-pixel checks pass. The PR's Vercel preview check passed. This evidence does not claim production deployment or production behavior verification, and no production database action ran.
- Current release (2026-08-21): the Oxalpha UI-report remediation and August 20 player-catalogue refresh are released and production-verified. PR #21 merged the implementation as `a033f3613f57c37720e2f0db51ada63965a4783c`; PR #22 merged the selector and protected-preview smoke fixes as `947118f5304271b5ae2a824329080b83d4d80723`.
- Current repository catalogue: the owner-selected August 20 handoff normalizes to 580 players across 20 clubs and 578 verified local portraits. Ryan McAidoo and Luc De Fougerolles intentionally use the silhouette fallback. The exact delta from August 18 is five additions, five removals, no intra-league moves, two position corrections, and two restored portraits for existing players.
- Production boundary: the owner-approved supported seed ran exactly once after the exact application deployment became Ready. Production now has 613 total player rows, 580 active rows, 33 preserved inactive rows, 578 active portrait paths, and the two documented silhouette fallbacks.

- Current phase: Win Streak is merged, seeded, deployed, and production-verified. This documentation closeout records the exact repository, migration, seed, deployment, alias, browser, log, and read-only database evidence.
- GitHub release: [PR #21](https://github.com/vdoshi96/Pl-predictions/pull/21) merged the UI and catalogue implementation as `a033f3613f57c37720e2f0db51ada63965a4783c`. [PR #22](https://github.com/vdoshi96/Pl-predictions/pull/22) merged the selector and production-smoke corrections as `947118f5304271b5ae2a824329080b83d4d80723`. [PR #24](https://github.com/vdoshi96/Pl-predictions/pull/24) merged administrator paste entry as `0a5ad854fe522993ff668ce06ffa94a4c9c0fed8`. [PR #26](https://github.com/vdoshi96/Pl-predictions/pull/26) merged the season-glance display as `ee325557b268510bafa9c7b54aa3b46cba509da4`. [PR #31](https://github.com/vdoshi96/Pl-predictions/pull/31) merged the dark-mode and dual-zone release as `eb1bc82a50f850ff5965c80d3d933e36e8c3f6b5`. [PR #33](https://github.com/vdoshi96/Pl-predictions/pull/33) merged the directional consensus fix as `0cf3caf9602bbbf10b2cf233c6676baf1005e9cc`. [PR #39](https://github.com/vdoshi96/Pl-predictions/pull/39) merged Win Streak as `19bf11b4df43f8c3410f704fb278d1d4bdd845b5`.
- Product: Dranx Prediction League uses one immutable three-stage entry: enter a display name and order all 20 clubs, choose seven spotlight predictions, then review and submit. Receipt privacy, derived leaderboard/comparison, owner settings and exact submission deletion, manual standings, authenticated source-neutral import, final-table controls, and the new manual results desk share one application.
- Local draft and table intent: a versioned `localStorage` draft keyed by season restores only a validated exact current-team permutation and valid partial category shapes after hydration. It survives validation/network/server failure and clears only after atomic success or verified permanent closure. The initial A–Z table is labelled as a blank slate; unchanged A–Z continuation requires a page-memory acknowledgement that Reset and restored drafts do not retain.
- Spotlight categories: top scorer, top assister, most clean sheets, underdog team, overrated team, underdog player, and overrated player. Player categories use searchable keyboard-accessible selectors plus Other-player text input; most clean sheets and both team-opinion categories use clubs and the existing crests. The local remediation requires two normalized search characters, announces the total match count, shows at most 20 matches, preserves Other player during loading or failure, and hides the sticky review action while a selector popup is open.
- Production player catalogue: the owner-selected August 20 handoff and live active fixture contain 580 players across the 20 clubs, 578 portrait paths, and null asset paths for Ryan McAidoo and Luc De Fougerolles. The handoff, normalized fixture, public assets, tracked fixture, and live rows reconcile internally; this does not claim independent official-club or Premier League roster verification. Other remains available, and `PlayerMark` falls back for an absent path or failed image.
- Local catalogue delivery remediation: the initial homepage server path reads explicit season fields and the 20 clubs without querying or serializing the player catalogue. Stage 2 lazily requests the dynamic same-origin catalogue route, which SQL-filters active current-season players and returns the minimal display/search shape. Submission validation queries only player IDs referenced by the submitted picks.
- Manual result remediation: `/admin/results` presents top scorer, top assister, club clean sheets, underdog player, and overrated player as five tables backed by four factual datasets. Shared player ratings synchronize category-filtered high/low views. A rating draft accepts the nonempty reviewed subset of resolved picked opinion players and rejects unpicked rows. Save creates a sealed immutable working snapshot with exact pinned Other-name aliases; publish/finalize/undo use CAS pointers and atomic audits. Publication requires permanent closure/reveal, at least one active bracket, every relevant alias, and explicit owner attestation. Goals, assists, and clean sheets retain current-`N` coverage with boundary ties. Missing or unresolved picked ratings display as N/A and remain unavailable for scoring.
- Administrator paste-entry iteration: `/admin/standings` accepts a reviewed 20-club text table only after alias resolution, numeric classification, complete-permutation validation, and an active-table diff. `/admin/results` can seed distinct submitted subjects, parse reviewed integer or rating lists, retain skipped lines for attention, and show an exact draft-versus-published diff before the owner attests. The combined action saves first and publishes the returned immutable snapshot; a publish failure leaves that saved working draft recoverable.
- Production remediation schema: preflight found the legacy active-season deadline `NULL`, 599 total/582 active players, zero normalized display-name duplicates, migration history through `0005`, and no result tables. Additive `0006`/`0007` were applied before the code release. A `READ ONLY` transaction followed by `ROLLBACK` verified their exact hashes (`1000ff0034a9954dbb4aeedcd26ece126b39adacc069d084c0d9945deb0c4be0` and `6b539a65a70a896bead07ba7010bd71834874bb3b3b1b5ae784536ef0f914db4`), all 8 migration rows, four pending states with every pointer `NULL`, zero result snapshots/items/aliases, the expected 3 unique indexes and 14 valid enabled triggers, zero closure/result audits, and false lock/reveal flags. No result fact was inferred or published, and the prior application remains compatible.
- Compatibility decision: use the exact refreshed portrait set with 17 obsolete paths removed and four club-move path renames. Do not retain legacy compatibility copies. The owner accepted the brief deploy-before-seed silhouette window; it ended when the seed completed. Historical database player rows remain because the seed deactivates absent fixture players instead of deleting them.
- Vercel: project `vdoshi96s-projects/pl-predictions`; deployment `dpl_8L2gqq4KjFiByeRrcVkXsWGFWjQQ` is Ready at [https://pl-predictions-plwwrm1l9-vdoshi96s-projects.vercel.app](https://pl-predictions-plwwrm1l9-vdoshi96s-projects.vercel.app), and [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app) resolves to that same deployment ID after explicit alias assignment and read-back.
- Recovery proof: the pre-seed read-only capture at 2026-08-21 15:51:21 UTC recorded WAL `0/9675540`, 20 teams, 599 total players, 582 active, 17 inactive, and 582 portrait paths. Neon point-in-time recovery uses the project's configured restore window; this timestamp and LSN are the exact rollback marker. No restore operation ran.
- Production seed and database: the supported seed ran exactly once and printed `Seeded 20 teams and 580 players for 2026/27 Premier League.` The post-seed capture recorded WAL `0/978E1E0`. Read-only verification passed with 20 teams; 613 total player rows; 580 active; 33 inactive; 580 distinct active external IDs and slugs; 578 portrait paths; exactly two null portrait paths; and zero active players without a team.
- Production browser: the guarded anonymous smoke passed 5 of 5 projects across desktop Chromium, 390-pixel mobile Chromium, exact 320/430-pixel reflow, and mobile WebKit. It covered the pointer-completion fix, current player names and portraits, Other player, the compact/expanded 20-club review, public routes, responsive containment, and browser/network/HTTP guards. Same-origin requests used only `GET`, `HEAD`, and `OPTIONS`.
- Production database postcheck: a transaction-scoped `READ ONLY` audit followed by `ROLLBACK` retained false lock/reveal flags, null standings pointers, zero result snapshots/items/aliases, and two existing audit rows. Safe aggregates remained 11 prediction parents, 220 table items, and 77 picks. The smoke issued no mutation request and wrote nothing.
- Production evidence: all 578 portrait URLs returned HTTP 200 PNG responses with valid signatures, and the live catalogue returned exactly 580 players plus the expected paths for representative August 20 additions. Seven production screenshots and four current isolated-remediation images were visually inspected as one completed-release set. Its deterministic 11-PNG manifest SHA-256 is `ce86bcde91820a97a11d08529ef6a592c9a3b03657a5c5e683f1999399fc2ce6`. Exact-deployment last-30-minute error, fatal, and HTTP 500 queries returned no records.
- Atomicity and deletion: a guarded prediction write accepts one parent only with all 20 ordered team rows and exactly seven spotlight rows, otherwise none persist. Authenticated exact deletion cascades through both child sets, preserves its deletion audit, and allows the normalized display name to submit again.
- Leaderboard and privacy: the main leaderboard uses table points only and has a 100-point maximum. `/spotlight` tracks separate fun accuracy. Before reveal, identifiers, positions 2–20, spotlight picks, and hidden accuracy ordering remain private. Only complete persisted entries contribute to the bracket count and eventual rankings.
- Scoring: the predicted champion remains table position 1 without a separate bonus. With `N` active, nondeleted brackets, spotlight accuracy is `max(0, N + 1 - outcome rank)` and sums only resolved categories. Equal overall scores share a competition rank. Each opinion category ranks only its distinct picked subjects. Team underdog uses `average predicted position - actual position`; overrated uses the inverse, with indexes still derived from every complete bracket.
- Production result inputs: the 2026-08-22 read-only preflight found active provisional snapshots for all four factual datasets that back the five result views: six snapshots, 109 items, zero pinned aliases, and no final dataset. The roster snapshot remains selector input rather than an outcome feed; future Other-player spellings still require exact canonical mapping before publication.
- Runtime boundary: the deployed application has no runtime football API, scraper, image hotlink, or Vercel Cron. Manual administrator result entry, manual standings, and the authenticated canonical standings importer preserve reviewed, last-good boundaries.
- Submission and security: the persisted opening kickoff remains `2026-08-21T19:00:00.000Z` and is the sole timed submission cutoff; the database clock sampled after the season-row lock remains authoritative. The legacy `submission_deadline` field is ignored by runtime, seed, environment, administrator, and guarded-insert paths. Earlier closure is available only through irreversible manual lock or early reveal, both of which reveal predictions and permanently close submissions. Administrator PBKDF2 login, signed cookies, same-origin mutation checks, WAF protection, receipt hashing, and the participant data-minimization boundary are unchanged.
- Previous roster-release verification: the uninterrupted `CI=1 npm run check` passed 14-document parity, the 582-player/portrait validator, formatting, ESLint, strict TypeScript, 205 unit/component tests with 11 guarded integration skips, all 11 isolated Neon integration tests, the Webpack production build, five pre-kickoff browser journeys, and three post-kickoff journeys. The separate focused set passed 77 tests, and the 33-tile additions/restorations/replacements/moves contact sheet passed visual inspection.
- August 14 remediation verification: full formatting and ESLint, strict TypeScript, Drizzle consistency, the then-current 582-player/portrait release fingerprint, 256 unit/component tests with 20 guarded skips, a brand-new isolated 0000–0007 migration and seed, all 18 Neon integration tests, the Webpack production build, 7 routed pre-kickoff browser journeys with 13 intentional skips, and 3 routed post-kickoff journeys with 2 intentional skips passed. The final aggregate `CI=1 npm run check` was not claimed as a pass because its dev-server Playwright stage encountered the documented Next Fast Refresh corruption after all earlier gates and build had passed.
- August 14 remediation cleanup: the clean merged feature worktree was removed normally, and the tracked evidence set remained. The isolated databases `pl_results_fresh_20260814_a`, `pl_results_sealed_20260814_b`, and `pl_results_sealed_20260814_c` were distinct from production and retained because `DROP DATABASE` is destructive and was not directly authorized.
- Private handoffs: the primary August 8 folder remains retained with independent full manifest `29d8dc2efda79ae5cde906214b91402008d89dbde71adca97e837aeab15e7ee3`, substantive non-`.DS_Store` parity manifest `e3c5d7c6ea5c75725fa4d3518333cd171e37d8a8cb407ecfa9fac0047bab3c5a`, and symlink path/target manifest `4e800f7c8103c59463469dfb492bdaaf1785a5f3e762a5427fc53596f540d888`. Its worktree duplicate matched substantive content and symlinks; only two Finder `.DS_Store` byte variants differed. The primary August 13 folder remains retained with independent full manifest `e1ee3b985b8e26799d0ec06632ebf9a1250ac1fab6e5749be0b4574a67aec83d`; original-algorithm manifest `f625470979c156e233300ec42924eb858255d902a456af1283a089ae41169630` remains valid, and its worktree duplicate was byte-identical.
- August 14 repository cleanup: the exact worktree duplicates moved recoverably to `/Users/vishal/.Trash/premier-league-players-2026-08-08-worktree-duplicate-2026-08-14` and `/Users/vishal/.Trash/premier-league-players-2026-08-13-worktree-duplicate-2026-08-14`. The feature worktree was removed normally and pruned. Local branches `roster/2026-08-13-player-update` and `vishal/august-13-player-catalogue-closeout` were deleted. Remote branches with those names plus `agent/dranx-brand-refresh` and `agent/dranx-release-closeout` were deleted only after ancestry proof. At that closeout, one primary worktree remained.
- QA and out-of-scope retention: superseded QA and hash-identical output screenshot duplicates moved recoverably to `/Users/vishal/.Trash/Pl-Predictions-superseded-QA-2026-08-14`. The worktree retains the freshly captured seven-production/four-isolated completed-release set; older production files were replaced under the newest-evidence rule. The unrelated untracked `Premier League 2026-27 PNG Assets/` folder remains intentionally retained and out of scope.
