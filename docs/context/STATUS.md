# Status

- Current phase: the how-to, deadline-countdown, complete-test-entry, and spotlight-fixture-retirement iteration is complete in production.
- Product: Dranx Prediction League now uses one immutable three-stage entry: order all 20 clubs and enter a display name, choose seven spotlight predictions, then review and submit. Existing receipt privacy, derived leaderboard/comparison, owner settings and exact submission deletion, manual standings, authenticated source-neutral import, and final-table controls remain in scope.
- Spotlight categories: top scorer, top assister, most clean sheets, underdog team, overrated team, underdog player, and overrated player. Player categories use searchable keyboard-accessible selectors plus Other-player text input; most clean sheets and both team-opinion categories use clubs and the existing crests.
- Player catalogue: the reviewed owner snapshot contains 587 players across the 20 clubs, with 580 imported local portrait PNGs and seven intentional `PlayerMark` silhouette fallbacks. Search accepts first, last, or full names. Other remains available for missing or newly added players. The raw handoff is never acquired or executed at runtime.
- Vercel: project `vdoshi96s-projects/pl-predictions`; stable production alias [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app).
- Current production deployment: Vercel deployment `dpl_2L4cprnsWShhesujY7sTLKffywhg` is Ready at [https://pl-predictions-9h7r762li-vdoshi96s-projects.vercel.app](https://pl-predictions-9h7r762li-vdoshi96s-projects.vercel.app). The stable alias resolves to that exact deployment; GitHub PR #13 merged the tested feature to `main` at `a7785b9b16b64be825e06ef8f181ab5f98031524`.
- Database: Vercel Marketplace Neon resource `neon-coffee-queen`; migrations `0000` through `0005` are applied to production. The idempotent seed maintains 20 teams plus 587 active players, including 580 portrait paths, seven null portrait fallbacks, and both cross-season integrity triggers. The retained `Dranx Test Entry` was created through the public flow and verified as one parent with 20 table rows and seven category picks.
- Atomicity and deletion: a guarded prediction write accepts one parent only with all 20 ordered team rows and exactly seven spotlight rows, otherwise none persist. Authenticated exact deletion cascades through both child sets, preserves its deletion audit, and allows the normalized display name to submit again.
- Leaderboard: the main leaderboard uses table points only and has a 100-point maximum. The separate `/spotlight` page tracks fun accuracy and supports overall or category sorting. Before reveal, identifiers, positions 2–20, spotlight picks, and hidden accuracy ordering remain private. The former Alex/Jordan cards were hard-coded component fixtures, never database rows, and are removed; only complete persisted entries contribute to the bracket count and eventual rankings.
- Scoring: the predicted champion remains table position 1 without a separate bonus. Spotlight accuracy never changes table points. Let `N` be the current number of active, nondeleted season brackets. Accuracy points are `max(0, N + 1 - outcome rank)`. Overall accuracy sums only resolved categories. A resolved zero-point result still counts as available. Pending outcomes remain unavailable. Equal overall scores share a competition rank.
- Team outcomes: underdog uses `average predicted position - actual position`, while overrated uses the exact inverse. Each list ranks the largest full-precision value first before applying the occupied-rank curve.
- Pending result inputs: a future owner-run Codex automation will enter top scorer, top assister, most clean sheets, underdog player, and overrated player manually. Player-opinion results use descending season-average rating for underdog and ascending rating for overrated. Custom Other-player names still need reconciliation. The roster snapshot is not an outcome feed.
- Standings: there is no runtime football API, scraper, or Vercel Cron. Manual administrator entry and the authenticated canonical importer preserve the last good complete 20-team permutation. FotMob automation remains prohibited absent written data-licence permission.
- Submission ceiling: the persisted 2026/27 opening instant remains `2026-08-21T19:00:00.000Z`. It is a non-extendable database-clock cutoff; an owner deadline may close earlier but not later. Once deadline, lock, or early reveal closes submissions, the season remains closed.
- Submission countdown: while entries are open, the hero status panel shows a compact calendar-flip days/hours/minutes/seconds timer. Its initial duration comes from database time and advances from `performance.now()`; it refreshes at zero, while the guarded PostgreSQL wall-clock check remains the only write authority.
- How to play: `/rules` now opens with three annotated 390 × 844 screenshots captured from the live mobile entry flow. Phones use snap scrolling, wide layouts use three columns, and each numbered visual pin has matching accessible callout text.
- Finalization: all 20 clubs must have 38 played games; finalization and undo compare-and-swap the exact active/final snapshot. Imports retain the monotonic accepted-through watermark and last-good protections.
- Administrator authentication: the configured username plus salted PBKDF2-SHA-256 hash succeeded on the post-removal deployment. Its metadata includes `ADMIN_USERNAME` and `ADMIN_PASSWORD_HASH` and excludes `ADMIN_SECRET`; the legacy name is also absent from all Vercel environment inventories. The raw owner password is absent from source, browser artifacts, logs, and documentation.
- Administrator WAF: live rule `rule_admin_login_protection_b4i7gM` limits `POST /admin/login` to 10 requests per 60 seconds for each IP. Excess requests receive HTTP 429. The post-publication read-back found active version 2 and no remaining draft.
- Local verification: the current feature revision passed uninterrupted `CI=1 npm run check`, including 150 default tests with 10 guarded skips, all 10 isolated Neon integration cases, the Webpack production build, five pre-kickoff browser journeys with 20 intentional routing skips, and three post-kickoff journeys with two intentional skips. The catalogue check proved 587 players, 580 portraits, and seven fallbacks. An earlier attempt stopped at Prettier before lint or tests and was corrected before this clean run.
- Production verification: the immutable deployment passed 5 of 5 read-only smoke projects across desktop Chromium, 390-pixel mobile Chromium, exact 320/430-pixel reflow, and mobile WebKit. A final aggregate found one `Dranx Test Entry` parent, 20 table items, seven spotlight picks, and no Alex/Jordan display-name rows. Exact-deployment error-level and HTTP 500 queries returned no records in the preceding 30 minutes.
- Mobile evidence: only the newest seven production artifacts are retained: countdown, prediction sorter, spotlight stage, final review, private spotlight state, rendered annotated how-to section, and administrator login. All were visually inspected. The three durable how-to source captures remain under `public/how-to-play/`.
- Access boundary: the stable public alias is available anonymously, while retained preview protection remains separate. GitHub reports `main` as the configured default branch. The fully merged legacy `agent/build-pl-predictions` remote branch is deleted, and local `origin/HEAD` points to `main`.
- Brand and assets: the original Dranx identity and palette remain. The owner-provided 20 local PNG club crests are canonical team marks, the original monograms remain rollback-only, and no Premier League logo/lion/ball asset is used. Player portraits are local reviewed selector assets with the generic silhouette fallback.
- Documentation and repository closeout: canonical Markdown and generated HTML record the current feature, test data, local/production verification, and newest-only visual evidence. The feature and closeout changes are published through GitHub `main`; local and remote `main` are synchronized, completed branch state is removed, and only the primary worktree remains. Raw owner handoff folders remain untouched and untracked.
