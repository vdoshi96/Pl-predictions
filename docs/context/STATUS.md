# Status

- Current phase: the seven-category spotlight prediction and reviewed player-catalogue feature is merged, live, and production-verified. PR #7 delivered the product at `c976c1719c8c17f8169b9c8b86248765821b203a`; PR #8 delivered the hardened release evidence at `a9d1c12c804ff34db48450ff2e57bded64710022`. The only open release control is owner publication of the staged administrator-login WAF rate limit.
- Product: Dranx Prediction League now uses one immutable three-stage entry: order all 20 clubs and enter a display name, choose seven spotlight predictions, then review and submit. Existing receipt privacy, derived leaderboard/comparison, owner settings and exact submission deletion, manual standings, authenticated source-neutral import, and final-table controls remain in scope.
- Spotlight categories: top scorer, top assister, most clean sheets, underdog team, overrated team, underdog player, and overrated player. Player categories use searchable keyboard-accessible selectors plus Other-player text input; most clean sheets and both team-opinion categories use clubs and the existing crests.
- Player catalogue: the reviewed owner snapshot contains 587 players across the 20 clubs, with 580 imported local portrait PNGs and seven intentional `PlayerMark` silhouette fallbacks. Search accepts first, last, or full names. Other remains available for missing or newly added players. The raw handoff is never acquired or executed at runtime.
- Vercel: project `vdoshi96s-projects/pl-predictions`; stable production alias [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app).
- Current production deployment: `dpl_HT2uPdP5iJPaWuYtNFEknroRmTqG` is Ready with target `production` at immutable URL [https://pl-predictions-dixkq8ma0-vdoshi96s-projects.vercel.app](https://pl-predictions-dixkq8ma0-vdoshi96s-projects.vercel.app). Its metadata identifies exact Git SHA `a9d1c12c804ff34db48450ff2e57bded64710022`; the stable alias points to it.
- Database: Vercel Marketplace Neon resource `neon-coffee-queen`; migrations `0000` through `0005` are applied to production. The pre-migration inspection found zero predictions and zero prediction items. The idempotent seed now maintains 20 teams plus 587 active players; verification found 580 portrait paths, seven null portrait fallbacks, and both cross-season integrity triggers.
- Atomicity and deletion: a guarded prediction write accepts one parent only with all 20 ordered team rows and exactly seven spotlight rows, otherwise none persist. Authenticated exact deletion cascades through both child sets, preserves its deletion audit, and allows the normalized display name to submit again.
- Leaderboard: before kickoff, real entries remain at 0 points and expose only display name plus predicted champion; identifiers, positions 2–20, and spotlight picks remain private. A clearly labelled visible test run demonstrates all spotlight categories with real player names, available portraits, club crests, and a deliberate silhouette fallback.
- Scoring: table scoring remains capped at 100. The current working category curve maps occupied ranks 1–20 to 20–1 points for a 240-point overall maximum; this mapping awaits owner confirmation. The predicted champion remains table position 1 without a separate bonus. Unavailable outcomes remain visibly pending rather than receiving zero.
- Team outcomes: underdog uses `average predicted position - actual position`, while overrated uses the exact inverse. Each list ranks the largest full-precision value first before applying the occupied-rank curve.
- Pending result inputs: top scorer, top assister, most clean sheets, underdog player, and overrated player still require reviewed source-neutral rankings. Player-opinion results use descending season-average rating for underdog and ascending rating for overrated. Custom Other-player names also need reconciliation to eventual outcome subjects. The roster snapshot is an identity-and-portrait source, not an outcome feed.
- Standings: there is no runtime football API, scraper, or Vercel Cron. Manual administrator entry and the authenticated canonical importer preserve the last good complete 20-team permutation. FotMob automation remains prohibited absent written data-licence permission.
- Submission ceiling: the persisted 2026/27 opening instant remains `2026-08-21T19:00:00.000Z`. It is a non-extendable database-clock cutoff; an owner deadline may close earlier but not later. Once deadline, lock, or early reveal closes submissions, the season remains closed.
- Finalization: all 20 clubs must have 38 played games; finalization and undo compare-and-swap the exact active/final snapshot. Imports retain the monotonic accepted-through watermark and last-good protections.
- Administrator authentication: the configured username plus salted PBKDF2-SHA-256 hash succeeded on the post-removal deployment. Its metadata includes `ADMIN_USERNAME` and `ADMIN_PASSWORD_HASH` and excludes `ADMIN_SECRET`; the legacy name is also absent from all Vercel environment inventories. The raw owner password is absent from source, browser artifacts, logs, and documentation.
- Administrator WAF: the live log-only login rule observed exactly four intended bounded-login `POST` requests. A 10-request-per-60-second, per-IP, HTTP-429 rate limit is staged but not live; owner publication remains pending.
- Local verification: the final tested feature revision passed `CI=1 npm run check`, including 147 default tests with 10 guarded skips, all 10 isolated Neon integration cases, the Webpack production build, five pre-kickoff browser journeys with 20 intentional routing skips, and three post-kickoff journeys with two intentional skips. The catalogue check proved 587 players, 580 portraits, and seven fallbacks.
- Production verification: the final hash-only deployment passed 5 of 5 read-only projects across desktop Chromium, mobile Chromium at 390 pixels, exact 320/430-pixel reflow, and mobile WebKit. Both demo details are explicitly opened before checking the decoded Haaland portrait and Alysson silhouette. The hardened bounded write smoke passed 1 of 1 against the same deployment, accepted only `PLAYWRIGHT_ADMIN_PASSWORD`, disabled screenshot/trace/video capture, retained only `.last-run.json`, causally bound the exact entry UUID to the configured database, and left zero parent, table-item, spotlight-pick, or deletion-audit residue.
- Mobile evidence: only the newest prediction, review, leaderboard, and administrator-login screenshots are retained. All four were refreshed at 390 by 844 pixels and visually inspected for the three-stage flow, real portraits, silhouette fallback, visible leaderboard demo, readable wrapping, reachable actions, and no horizontal clipping.
- Access boundary: the stable public alias is available anonymously, while retained preview protection remains separate. No GitHub default-branch setting is asserted; `main` is the canonical working branch for this release.
- Brand and assets: the original Dranx identity and palette remain. The owner-provided 20 local PNG club crests are canonical team marks, the original monograms remain rollback-only, and no Premier League logo/lion/ball asset is used. Player portraits are local reviewed selector assets with the generic silhouette fallback.
- Documentation and repository closeout: canonical Markdown and generated HTML record the verified spotlight release. Final evidence is published through working `main`, local and remote `main` are synchronized, only the primary worktree remains, and raw owner handoff folders remain untouched and untracked. WAF publication and the explicitly listed scoring/outcome inputs remain open owner decisions rather than hidden implementation claims.
