# Session log

## 2026-08-31

- Verified the official Win Streak source with no drift: 370 fixtures across 37 matchweeks and the retained August 28 source date. No fixture rewrite, release, or targeted production seed ran.
- Confirmed production season `2026-27`, `N = 14`, 20 clubs, 37 Win Streak rounds, 370 fixtures, null final pointers, and zero resolved Win Streak rounds before mutation.
- Resolved FotMob season metadata dynamically to `2026/2027` and stats path `stats/47/season/36781/`. Published the 20-club standings table and provisional goals, assists, clean-sheets, and player-ratings snapshots through the authenticated administrator desks.
- Reconciled the sealed active and working pointers: goals `fc93b34a-77d6-41ae-bc35-fcbd95e600a0`, assists `114b6a29-1b8c-46a7-8714-c9b9a1b1cfc7`, clean sheets `b1e038a1-cf6b-4ac8-bfe1-7c241b81e388`, and ratings `e364b977-b15e-4ef0-ba54-790e99af1b51`. Four picked players remain unavailable as N/A.
- Published all 10 reviewed Matchweek 2 outcomes atomically with no voids. Both existing Manchester United picks won, so Rohan Tejaswi and Vishal Doshi share first with current and best streaks of one. Matchweek 3 is open, and both profiles show no current pick.
- Verified the stable production domain on Ready deployment `dpl_HLqSB2C8vJpyMUpEeajMhY8YBabd`. The rendered table is led by Adi Ashok on 39 points, and Keshav leads Spotlight with 82 points. Anonymous desktop and 390-pixel browser checks returned `200` without console warnings or failed application requests.
- Recorded a follow-up defect: Next.js 16.3 rejects the current `/admin/win-streak` server-action module because the `"use server"` file also exports a client initial-state object. A temporary local-only module split enabled the authenticated action and was removed after the successful write; no application source change remained.
- Preserved the release boundary: no finalization, `LOCK`, `REVEAL`, runtime football client, direct result write, schema migration, fixture seed, protected-round change, or unrelated application release ran.

## 2026-08-30

- Verified the official Win Streak source with no drift: 370 fixtures across 37 matchweeks and the retained August 28 source date. No fixture rewrite, release, or targeted production seed ran.
- Confirmed production season `2026-27`, `N = 14`, 20 clubs, 37 Win Streak rounds, 370 fixtures, null final pointers, and zero resolved Win Streak rounds before mutation.
- Resolved FotMob season metadata dynamically to `2026/2027` and stats path `stats/47/season/36781/`. Published the 20-club standings table and provisional goals, assists, clean-sheets, and player-ratings snapshots through the authenticated administrator desks.
- Reconciled every active fact with zero mismatches. Goals snapshot `5c8287d4-2443-446a-8ce3-7fe39e134f8c`, assists `43c1084b-4dc9-4347-a0b0-8ae8c327de7a`, clean sheets `367f4e8b-64fa-496f-8507-b6ba6c5dcec4`, and ratings `496bbada-9393-447e-a9f5-1e419b2ed625` are sealed, active, working, provisional, and sourced to FotMob. Five picked players remain unavailable as N/A.
- Kept Matchweek 2 unresolved because Aston Villa-Arsenal is not final. Nine reviewed fixtures are complete, but the all-10 gate forbids a partial transition. No outcome or void was stored.
- Verified the stable production domain on Ready deployment `dpl_D6fxyz1ixQ5DGRDXc9rbYpvuurwJ`. The rendered table is led by Angad on 36 points; Aravind and Keshav share the 80-point Spotlight lead. Both Win Streak profiles remain tied at 0 with Manchester United picks. Desktop and 390-pixel checks returned `200` without console errors, failed requests, mutation requests, or horizontal overflow.
- Preserved the release boundary: no finalization, `LOCK`, `REVEAL`, runtime football client, direct result write, schema migration, protected-round change, or unrelated application work ran.

## 2026-08-28

- Accepted the owner decision to publish reviewed player-rating subsets. The admin desk permits a nonempty subset of picked opinion players, rejects unpicked rows, and explains how many omitted players will publish as N/A. Public opinion-player views label missing ratings N/A and continue to exclude them from accuracy scoring.
- Published the implementation through [PR #46](https://github.com/vdoshi96/Pl-predictions/pull/46) as `e852d717ed6af12cd54916e5e0ec5475abce3f98`; exact Ready deployment `dpl_5bVFdeCYjckhPwPWe2gSHYMo6YMi` serves the automatic project and main aliases. The separately assigned `pl-predictions-2026.vercel.app` alias remains on August 24 deployment `dpl_5znWdTiecQjPMyyS56qBVc4YiAcv` pending explicit owner approval to move it.
- Published sealed provisional player-rating snapshot `3bd5c440-0add-4de2-800c-e5757e21efea` through the authenticated admin desk. Read-only reconciliation matched all 15 reviewed facts, retained seven unrated picked players as absent N/A values, and confirmed 22 picked players, `N = 14`, active equals working, and no final pointer. Exact-deployment browser checks verified every category, Keshav's 90-point overall lead, mixed six-of-seven and seven-of-seven availability, and the unchanged two-entry Win Streak state.

## 2026-08-24

- Implemented separate distinct picked-subject pools for underdog player, overrated player, underdog team, and overrated team. Team metrics still use every complete bracket, while each candidate list is category-specific. Player directions filter the same active sealed rating facts independently; a missing picked rating remains pending.
- Updated the administrator ratings desk to show category-filtered pools, synchronize shared-player edits, seed the union once, ignore unpicked pasted rows with a count, require exact picked-player completeness, and preserve `covered_through_rank = N`. The strengthened 320/430-pixel journey exposed a 526-pixel populated-table overflow; stacked mobile editor rows fixed it.
- Passed documentation parity for 18 pairs, the 580-player/578-portrait validator, formatting, ESLint, strict TypeScript, 419 unit/component tests with 33 guarded skips, all 31 isolated Neon integration tests, the Webpack production build, and focused desktop/320/430-pixel optimized-browser journeys across Spotlight, entry details, and administrator results.
- Published through [PR #43](https://github.com/vdoshi96/Pl-predictions/pull/43), which passed both Vercel checks and merged as `0abe166dc043820dc5201c9d6219b52a3fb39742`. Ready deployment `dpl_5znWdTiecQjPMyyS56qBVc4YiAcv` at [its immutable URL](https://pl-predictions-5qo9i5lbh-vdoshi96s-projects.vercel.app) received the [stable production alias](https://pl-predictions-2026.vercel.app), and deployment-ID read-back proved the mapping.
- Passed the guarded five-browser production smoke with only read-only same-origin methods. A separate raw-SQL computation of the four candidate pools matched 112 rendered assignments across desktop and 390-pixel mobile, including pending missing ratings and exact page containment.
- Captured production read-only at `2026-08-25T02:23:18.996448Z`, WAL `0/FA28990`. No tracked season row was created or updated after deployment. The 14/280/98 prediction shape, active standings pointer with eight snapshots and 160 facts, exact result pointers with 32 snapshots and 1,883 immutable facts, zero aliases, and 75 existing audits remained intact. Bounded exact-deployment error, fatal, and HTTP-500 queries were empty. No migration, seed, result republish, snapshot transition, or production database mutation ran.

## 2026-08-23

- Completed an interaction-first UI/UX audit of all 13 public, participant, receipt-only, login, and authenticated administrator routes using desktop and mobile Chromium. The manifest records 117 entries, 47 interaction-first snapshots, 113 screenshots, 35 console reads, and 30 network probes. Production was read-only; participant round trips and administrator previews used the isolated test database.
- Fixed the Rules walkthrough's serious `scrollable-region-focusable` violation and eight-pixel document overflow at 768 pixels by naming and focusing the internal carousel and removing its escaping medium-breakpoint margin. Added shared, reduced-motion-safe state feedback for searchable dropdowns, prediction-stage panels, participant and administrator confirmation dialogs, overlays, and the final success mark.
- Re-ran every public and administrator route against the optimized local build with zero Critical or Serious axe violations, console warnings, console errors, HTTP errors, or document overflow. Exact responsive sweeps passed at 320, 375, 768, 1,024, 1,280, and 1,920 pixels; offline draft recovery, immutable prediction and Win Streak round trips, focus traps, keyboard operation, and reduced-motion behavior also passed.
- Cleaned only the isolated audit records by exact normalized names: two prediction parents, 40 table rows, 14 spotlight picks, two Win Streak profiles, and two Win Streak picks. Cascade read-back found zero residue, and Win Streak rate-limit rows plus administrator sessions were zero afterward.
- Passed the uninterrupted final `CI=1 npm run check`: parity for 18 documentation pairs, the 580-player/578-portrait validator, formatting, ESLint, strict TypeScript, 414 unit/component tests with 31 guarded skips, all 29 isolated Neon integration tests, the Webpack production build, 24 pre-kickoff browser journeys with 26 intentional routing skips, and three post-kickoff journeys with two intentional skips. The optimized browser server emitted the repository's documented destination-stream closure messages while every assertion passed.
- Published the remediation through [PR #41](https://github.com/vdoshi96/Pl-predictions/pull/41), which passed both Vercel checks and merged as `a09798709e461fff511ea4712a3b1a8bb9649f86`. Ready production deployment `dpl_2c3dpxwrtkPhQNbqRRyga8ti3QDb` at `https://pl-predictions-d35jddef5-vdoshi96s-projects.vercel.app` maps to that exact merge in GitHub's production deployment record.
- Passed the guarded five-browser read-only smoke against the exact deployment, explicitly assigned `https://pl-predictions-2026.vercel.app`, proved the alias resolved to the same deployment ID, and passed the five-browser smoke again through the alias. The final 12-route desktop/mobile audit found zero axe violations, overflow, console warnings/errors, or HTTP failures; 15-minute exact-deployment error, fatal, and HTTP-500 log queries were empty. The final after screenshots were recaptured from the stable site. No production mutation request ran.
- Recorded one unresolved medium product decision: the receipt authorizes a direct private confirmation, but public surfaces intentionally expose no unrevealed entry identifier and therefore cannot rediscover that URL after the success screen. The audit recommends a separately reviewed, privacy-preserving recovery design rather than weakening the existing disclosure boundary.

- Promoted the browser-local Win Streak workshop into a durable Matchweek 2–38 production game. Added 37 rounds and 370 canonical official fixtures, receipt-bound no-login profiles, database-clock pick deadlines, immutable one-per-round picks, derived streak scoring, public current picks, and a public anonymous leaderboard.
- Added the authenticated `/admin/win-streak` results desk. It resolves only the earliest unresolved round after all ten kickoffs, requires ten explicit outcomes plus reviewed provenance, and publishes the complete round in one immutable audited database transition.
- Extended the owner-run update-results skill and its generated HTML peer to check official fixture drift before results. No change is a no-op. Only a future, unpicked, unresolved kickoff correction may use the targeted seed path; structural or protected-fixture changes fail closed.
- Ran the online official fixture check on August 23. It verified all 370 retained fixtures across 37 matchweeks with no drift, so no fixture rewrite or seed ran at that checkpoint.
- Added migration `0010_win_streak_live.sql`, a targeted production fixture seed, persistent mutation limits, bounded public reads, concurrent-publication consistency checks, and tests for schedule coverage, identity takeover, outcome transitions, shared results, fixture drift, privacy, and atomicity.
- Passed the full 413-test unit/component suite with 31 guarded skips, all 29 isolated Neon integration tests, ESLint, strict TypeScript, and the Webpack production build. The focused optimized browser matrix passed 16 journeys across desktop Chromium, 320-, 390-, and 430-pixel mobile Chromium, and mobile WebKit, with four intentional project skips. It covered anonymous leaderboard access, public picks, two profiles with opposing fixture picks, keyboard confirmation, persistence, dark mode, and zero horizontal overflow.
- Ran the literal `CI=1 npm run check`. Its first development-server browser phase reproduced the existing Next.js RSC, manifest, Fast Refresh, and router-initialization corruption after every non-browser gate passed. Updated only the aggregate gate to exercise the production build it already creates, while retaining direct `npm run test:e2e` as the development-server diagnostic path. The corrected check passed uninterrupted: 17 documentation pairs, the player validator, formatting, ESLint, strict TypeScript, 413 unit/component tests with 31 guarded skips, all 29 isolated Neon integration tests, the Webpack production build, 24 pre-kickoff browser journeys with 26 intentional skips, and three post-kickoff journeys with two intentional skips.
- Read back the isolated database after the aggregate run: 37 rounds and 370 fixtures remained; resolved rounds, Win Streak profiles, picks, Win Streak rate limits, post-kickoff predictions, and post-kickoff snapshots were all zero; season pointers remained null and lock/reveal flags remained false. No server remained on port 3100.
- Matchweek 2 begins on August 28, 2026, so no Win Streak result backfill existed on August 23.
- Passed the repeatable-read production preflight at `2026-08-23T20:18:10.429Z`, WAL `0/E8E6438`. It found ten known migrations, 14 prediction parents with 280 table rows and 98 spotlight picks, intact standings/result state, false lock/reveal flags, and no Win Streak tables. Applied additive migration `0010`; read-only postcheck at WAL `0/E9185F8` verified exact hash `ce4aa426b4f91c85de7a126134a59ad8839374cf86cc1a278f17166be6f19134`, 28 valid constraints, 19 indexes, 12 triggers, 10 functions, and unchanged existing aggregates.
- Ran only `npm run db:seed:win-streak`. It inserted 37 rounds and 370 fixtures. Read-only postcheck at WAL `0/E979C38` verified Matchweeks 2–38, ten fixtures and 20 clubs per round, earliest-kickoff deadlines, zero results/profiles/picks/rate limits, and unchanged original-game, standings, result, audit, and season-flag state.
- Merged [PR #39](https://github.com/vdoshi96/Pl-predictions/pull/39) as `19bf11b4df43f8c3410f704fb278d1d4bdd845b5`. Vercel deployment `dpl_BTncZC77GmA3EXtaUjAKvfWZvRzP` became Ready at [its immutable URL](https://pl-predictions-cdq31rqls-vdoshi96s-projects.vercel.app). Explicitly assigned the [stable production alias](https://pl-predictions-2026.vercel.app) and proved it resolved to that deployment ID.
- Passed the guarded five-browser anonymous production smoke, including `/win-streak`, with no mutation requests, console errors, or horizontal overflow. Final database read-back found no participant or result writes and unchanged existing aggregates. Bounded exact-deployment error, fatal, and HTTP 5xx log queries returned no records.

## 2026-08-22

- Released directional season-table consensus deltas through [PR #33](https://github.com/vdoshi96/Pl-predictions/pull/33), merged as `0cf3caf9602bbbf10b2cf233c6676baf1005e9cc`. Every nonzero one-decimal delta uses `▲` or `▼`; only displayed `0.0` remains neutral.
- Passed formatting, ESLint, strict TypeScript, 347 enabled unit/component tests, all 22 isolated Neon integration tests, the Webpack production build, and the three-project optimized post-kickoff browser matrix. A prediction-form draft-cleanup timing failure in the first full unit run passed in isolation and in the complete rerun.
- Verified Ready production deployment `dpl_8L2gqq4KjFiByeRrcVkXsWGFWjQQ` at [https://pl-predictions-plwwrm1l9-vdoshi96s-projects.vercel.app](https://pl-predictions-plwwrm1l9-vdoshi96s-projects.vercel.app), assigned [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app), and proved the alias resolved to the same deployment ID.
- Passed the bounded five-browser anonymous smoke. A live independent audit matched all 20 consensus rows, including Arsenal `▲ 0.1` and Brentford `▲ 0.3`; the 390-pixel view had no horizontal overflow, the browser console was clean, and deployment error and HTTP 5xx log queries returned no records. No migration, seed, authenticated mutation, or production database write ran.
- Released the dark-mode and dual-zone time implementation through [PR #31](https://github.com/vdoshi96/Pl-predictions/pull/31), merged as `eb1bc82a50f850ff5965c80d3d933e36e8c3f6b5`. The change implements report items B, C, and D: system-preference light/dark palettes, browser theme metadata, explicit light walkthrough frames, bordered leaderboard tracks, and an unclipped top-three podium with accessible first-to-third document order and desktop `2–1–3` visual placement.
- Replaced site-rendered UTC-only and viewer-local date-time labels with deterministic Chicago-local-plus-UTC text. Administrator deadline comparison retains an optional selected third time zone after the Chicago-and-UTC baseline.
- Passed formatting, ESLint, strict TypeScript, 346 enabled unit/component tests, all 22 isolated Neon integration tests, and the Webpack production build. The optimized local production server passed seven pre-kickoff journeys, a focused mobile WebKit rerun, and three post-kickoff journeys; the development-server aggregate retained its documented response-stream corruption boundary.
- Verified Ready production deployment `dpl_252wwia4AqFJevuPhTiWZrwwR3a7` at [https://pl-predictions-75wicw45f-vdoshi96s-projects.vercel.app](https://pl-predictions-75wicw45f-vdoshi96s-projects.vercel.app). The automatic production aliases advanced while the documented stable alias still served the prior deployment, so explicitly assigned [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app) and proved it resolved to the new deployment ID.
- Passed the bounded five-browser anonymous smoke after the alias update. Live desktop and 390-pixel browser checks verified both palettes, Chicago-plus-UTC text, podium and progress details, no horizontal overflow, and zero console errors. Deployment error and HTTP 5xx log queries returned no records. No migration, seed, authenticated mutation, or production database write ran.
- Reconciled `adversarial-review-2026-08-22.html` and `visual-qa-issues-2026-08-21.html` against current `main`, then continued the half-finished result-validation change in a dedicated worktree without modifying unrelated primary-worktree material.
- Added persistent administrator login throttling and revocable sessions, standings-ingest throttling and bounded bearer rotation, a per-response nonce CSP without script `unsafe-inline`, production HSTS, fail-closed local-QA flags on Vercel, invisible/control-character rejection, shared catalogue caching, and migrations `0008`/`0009` for the new security rows and standings invariants.
- Moved the standings importer into application source, left the script as a thin CLI, removed the dead champion component, shared root season context through React request caching, and ignored the supplied review/reproduction artifacts.
- Corrected public result and provisional-tie language, explicit result-rank labels, mobile deadline and sticky-action presentation, narrow-name wrapping, current August 20 roster facts, and the closed-form public state. Recaptured and visually inspected all three walkthrough sources from the current 390 × 844 flow, with Stage 1 and Stage 2 now beginning at their headings rather than midway through controls.
- Rebuilt the isolated Neon database from migrations `0000`–`0009`, seeded 20 teams and 580 players, and passed all 22 integration tests. Documentation parity for 16 pairs, the exact player/portrait validator, Prettier, ESLint, strict TypeScript, all 343 enabled unit/component tests, and the Webpack production build pass.
- Passed eight routed pre-kickoff production-server journeys across desktop Chromium, 390-pixel mobile Chromium, exact 320/430-pixel Chromium, and iPhone WebKit, plus three routed fixed post-kickoff journeys at desktop, 320, and 430 pixels. A final aggregate audit returned zero QA predictions, post-kickoff snapshots, and rate-limit rows. Deleted the seven isolated administrator sessions created by browser login and verified zero active-session residue. Production remains unchanged pending migration and release.
- Passed the transaction-scoped read-only production preflight at `2026-08-22T07:13:15.757Z`, WAL `0/C60F2B8`. It found eight migrations through `0007`; 14 prediction parents with exact 20-item/seven-pick shapes; one complete standings snapshot; false manual lock/reveal flags; 580 active players and 578 portraits; six active provisional result snapshots with 109 items, zero pinned aliases, and no final dataset; and no `admin_sessions` or `security_rate_limits` tables. Corrected the stale all-results-pending documentation before mutation.
- Opened [PR #28](https://github.com/vdoshi96/Pl-predictions/pull/28). Its Vercel preview `dpl_8RBVHivijbEVQKMbLh12zUNaKX9x` became Ready, and Vercel-authenticated read-only requests returned HTTP 200 for health, root, Rules, and Spotlight with the expected health JSON. The five-browser preview attempt reached Vercel's HTML protection interstitial because no automation-bypass secret is configured; it is not counted as application QA.
- Applied additive production migrations `0008_previous_ezekiel_stane.sql` and `0009_condemned_nighthawk.sql` before code publication. A transaction-scoped read-only postcheck at `2026-08-22T07:17:21.326Z`, WAL `0/C61CE10`, verified ten migration rows and exact new hashes `f00242a1262d1fcab778021ba3d1bd38147ed5bb780f459cfa2c184baad87300` and `f004e2d32c5d7923b7b50c28dbe3e3093c59ac70593ba7b6eef62954a7d8f287`. Both security tables exist; all participant, standings, result, player, flag, and pointer aggregates match the preflight exactly.

## 2026-08-21

- Published the season-glance implementation through [PR #26](https://github.com/vdoshi96/Pl-predictions/pull/26), merged as `ee325557b268510bafa9c7b54aa3b46cba509da4`. Both Vercel checks passed. Local `main` and `origin/main` matched the merge exactly before this documentation-only closeout, the remote feature branch was deleted, and no production database or production deployment action ran.
- Implemented the approved season-glance display. The unrevealed root still renders the existing three-stage form; the revealed root shows the current 20-club table, consensus averages, expectation deltas, and overachiever/underachiever callouts without exposing consensus-bearing data before reveal.
- Replaced the leaderboard card stack with a top-three podium and dense real table. Shared ranks repeat, deterministic initials support non-Latin names, progress remains capped at 100, and movement compares canonical scores with the previous meaningful standings snapshot.
- Split Spotlight into default category boards, the retained per-entry list, and a sticky-column matrix. Category leaders come only from active sealed factual snapshots or active standings scoring; pending and attested outside-range states remain distinct.
- Passed 338 unit/component tests with 21 guarded skips, all 21 isolated Neon integration tests, strict TypeScript, ESLint, and the production build during phased verification.
- Passed the local production-server pre-kickoff routes across desktop/mobile Chromium, exact 320/430-pixel reflow, and mobile WebKit, plus the post-kickoff season-glance journey in desktop and exact-width Chromium. The literal final `CI=1 npm run check` passed every non-browser gate, then its development-server phase reproduced router initialization, truncated JSON, Fast Refresh reload, and selector-loss failures: 3 passed, 22 intentionally skipped, 4 failed, and 1 flaky after retries. The development-server aggregate is not claimed as a browser pass.
- Verified the completed browser runs left zero active-season predictions and zero post-kickoff QA snapshots in the isolated database. No production data, deployment, schema, dependency, or environment variable changed.
- Published the administrator paste-entry implementation through [PR #24](https://github.com/vdoshi96/Pl-predictions/pull/24), merged as `0a5ad854fe522993ff668ce06ffa94a4c9c0fed8`. Its Vercel preview checks passed. The merge did not authorize or run any production database action, and production behavior remains outside this local and preview verification record.
- Implemented the approved administrator paste-entry design on `feature/admin-paste-entry`: shared normalization and aliases, guarded standings parsing and diffing, result-list parsing, current-bracket coverage and boundary-tie checks, submission-derived subject seeding, paste panels, and a combined review, attestation, save, and exact-snapshot publish flow.
- Preserved the existing server-action, session, same-origin, immutable snapshot, CAS, alias, and atomic standings boundaries. A successful save followed by a failed publish remains a recoverable clean working draft. No production data, seed, result pointer, standing, prediction, or deployment changed.
- Passed documentation and catalogue checks, ESLint, strict TypeScript, 317 unit/component tests with 20 guarded skips, all 20 isolated Neon integration tests, and both the normal and local-QA production builds. The local production-server browser harness passed eight pre-kickoff routes with 22 intentional skips and three post-kickoff routes with two intentional skips.
- Visually inspected the new standings and results paste controls plus a 15-row publication diff at desktop, 375 pixels, and 320 pixels. Document horizontal scroll remained zero, wide tables stayed internally scrollable, dialog actions remained reachable, and tested actions measured at least 44 pixels high. The synthetic isolated bracket was removed with zero parent, item, or pick residue.
- Reset the superseded unmerged UI branch to verified `main`, deleted that local branch, and created `/private/tmp/pl-predictions-ui-report-remediation` on `vishal/ui-report-remediation`. Unrelated private handoffs and the Oxalpha report remain in the primary checkout.
- Implemented the 20 UI-report findings, including compact mobile layout, viewport-contained selectors, debounced draft persistence with lifecycle flushing, expanded keyboard movement, stronger drag feedback, polite announcements, first-error spotlight validation, compact review and leaderboard layouts, one-second countdown alignment, viewport metadata, and reusable design tokens.
- Normalized the owner-provided August 20 roster without executing acquisition scripts. Generated 580 selector rows and 578 verified 192-by-192 portraits, with intentional silhouette fallbacks for Ryan McAidoo and Luc De Fougerolles. Enforced exact source, identity-set, transition, release-fixture, and portrait-fingerprint checks against the August 18 baseline.
- Passed formatting, ESLint, strict TypeScript, the full 264-passed/18-skipped unit and component suite, all 18 isolated Neon integration cases, the exact 580-player/578-portrait catalogue validator, and the Webpack production build.
- Passed the production-server pre-kickoff matrix with 7 routed journeys and 13 intentional project skips across desktop/mobile Chromium, exact 320/430-pixel participant and administrator reflow, and mobile WebKit. Passed the fixed-clock post-kickoff matrix with 3 routed Chromium journeys and 2 intentional skips. The explicit `LOCAL_HTTP_E2E=1` harness permits local HTTP CSP, cookies, and fixed time only for this isolated production-build QA path; ordinary production builds retain secure-cookie, HTTPS-upgrade, and database-clock behavior.
- Read back the final normal production build's `/rules` response as HTTP 200 with the expected CSP, including `upgrade-insecure-requests` and no development-only `unsafe-eval`.
- Recaptured and visually inspected the three 390-by-844 mobile walkthrough images. Stage 3 now documents the compact top-5, collapsed-middle, and bottom-3 table review rather than duplicating the Stage 2 spotlight view.
- Published the implementation through [PR #21](https://github.com/vdoshi96/Pl-predictions/pull/21), merged as `a033f3613f57c37720e2f0db51ada63965a4783c`, and deployed it before the database change. Captured a read-only pre-seed marker at 2026-08-21 15:51:21 UTC and WAL `0/9675540`, then ran the approved supported production seed exactly once.
- The seed produced 580 active players, 33 preserved inactive rows, 578 portrait paths, and the two expected silhouettes. Prediction aggregates stayed at 11 parents, 220 table rows, and 77 spotlight picks; lock/reveal, standings, result, and audit state did not change.
- The first production smoke exposed a pointer-completion regression and a stale compact-review assertion. Fixed both through [PR #22](https://github.com/vdoshi96/Pl-predictions/pull/22), added support for the existing Vercel automation bypass on protected previews, and kept the CSP exception limited to Vercel's injected preview-comments script.
- PR #22 merged as `947118f5304271b5ae2a824329080b83d4d80723`. Vercel deployment `dpl_J3We57iVsWDMbKSFkoCDTi62mhCz` became Ready at [its immutable URL](https://pl-predictions-9z25e3j3o-vdoshi96s-projects.vercel.app) and passed stable-alias deployment-ID read-back.
- Passed the final anonymous read-only smoke in all five browser projects. Verified all 578 portrait URLs as valid PNG responses, the live 580-player catalogue and representative August 20 additions, unchanged post-smoke database aggregates, and zero bounded error, fatal, or HTTP 500 log records.
- Replaced the seven retained production screenshots, preserved the four current isolated-state captures, visually inspected the complete 11-image set, and recorded manifest SHA-256 `ce86bcde91820a97a11d08529ef6a592c9a3b03657a5c5e683f1999399fc2ce6`.

## 2026-08-14

### Adversarial-review remediation (production release complete; evidence closeout pending)

- Implemented the approved fixes for findings 3 and 8–13: validated season-keyed browser drafts, explicit A–Z intent confirmation, lazy and narrowly projected player-catalogue delivery, two-character/20-result selectors, participant-facing copy cleanup, a five-table manual results desk backed by four factual datasets, and fixed-kickoff administrator controls with typed irreversible closure confirmation. Findings 1, 2, 4, 5, 6, and 7 remained intentionally out of scope.
- Applied migrations `0000` through `0007` cleanly and completed the supported seed against fresh isolated database `pl_results_sealed_20260814_c`. The final result-focused run passed 4 of 4 cases, and the complete isolated Neon suite passed all 18 cases.
- Passed the complete local unit/component suite with 256 tests and 20 guarded integration skips, full formatting and ESLint, strict TypeScript, Drizzle schema checking, the unchanged 582-player/portrait release fingerprint, `git diff --check`, and the Webpack production build.
- Passed the complete isolated browser matrix: 7 pre-kickoff journeys with 13 intentional project-routing skips across desktop/mobile Chromium, exact 320/430-pixel participant/admin reflow, and mobile WebKit; plus 3 fixed-clock post-kickoff journeys with 2 intentional skips. Exact cleanup assertions left no QA prediction, standings, audit, or season-state residue.
- Recaptured and visually inspected the three 390 × 844 public walkthrough sources and four new remediation images for A–Z confirmation, complete draft restore, unsaved manual-results editing, and the fixed-kickoff settings desk. No result Save/Publish/Finalize, production submission, Lock, or Reveal action was invoked.
- Regenerated 11 stale HTML peers and passed deterministic parity for all 14 Markdown/HTML document pairs.
- Ran a final `CI=1 npm run check` attempt. It completed documentation and player checks, formatting, ESLint, strict TypeScript, the 256-passed/20-skipped unit suite, all 18 isolated Neon integration cases, and the Webpack production build, then failed in the isolated development-server Playwright stage after repeated Next Fast Refresh reloads, truncated JSON responses, and router-initialization corruption. This is not recorded as an aggregate pass; the equivalent standalone production-server Chromium matrix (including exact 320/430-pixel coverage) and mobile WebKit journey, plus the separate fixed-clock post-kickoff matrix, remain independently green.
- Completed the production migration preflight read-only: the active season's legacy deadline was `NULL`; the catalogue held 599 total and 582 active players; normalized display names had zero duplicate groups; migration history ended at `0005`; and no result tables existed.
- Applied additive migrations `0006_complex_ultimatum.sql` and `0007_brainy_harpoon.sql` before deploying code that uses them. A transaction-scoped `READ ONLY` verification followed by `ROLLBACK` proved all 8 migration rows and exact committed hashes (`0006`: `1000ff0034a9954dbb4aeedcd26ece126b39adacc069d084c0d9945deb0c4be0`; `0007`: `6b539a65a70a896bead07ba7010bd71834874bb3b3b1b5ae784536ef0f914db4`), four pending dataset states with all working/active/final pointers `NULL`, zero result snapshots/items/aliases, the expected 3 unique indexes and 14 valid enabled triggers, zero closure/result audits, and both season closure flags false. The migration inferred or published no result facts and left the August 13 application compatible.
- Merged feature [PR #19](https://github.com/vdoshi96/Pl-predictions/pull/19) on 2026-08-14 as `9cdad64c285fb66f96d9e11a0b71780008b05160`. Vercel deployment `dpl_2evvhgizD4spM9QfXKNhvHLEBrgG` became Ready at `https://pl-predictions-msrzm2z84-vdoshi96s-projects.vercel.app`, reported that exact merge SHA, and was explicitly assigned to the stable production alias; deployment-ID read-back proved the alias resolved to it.
- Passed the final five-project read-only production smoke across desktop Chromium, 390-pixel mobile Chromium, exact 320/430-pixel Chromium reflow, and mobile WebKit. The same-origin guard observed only `GET`, `HEAD`, and `OPTIONS`; no result Save/Publish/Finalize/Undo, participant submission, Lock, or Reveal action ran.
- Completed a post-smoke production database audit inside a `READ ONLY` transaction followed by `ROLLBACK`. It retained legacy deadline `NULL`, false lock/reveal flags, four pending states with every pointer `NULL`, zero result facts/aliases/audits, exact migration parity, and the safe aggregate two-parent/40-table-item/14-pick participant shape. The audit wrote nothing.
- Queried the exact deployment over the bounded 30-minute smoke window and found no error records, no fatal records, and no HTTP 500 records.
- Freshly captured and visually inspected seven production screenshots, retained them with the four current isolated captures as one 11-image completed-release evidence set, and recorded deterministic manifest SHA-256 `fc0087cf9073ba43759d82ffea40b9e1fd5088c08112d3c6a32ed4486964f337`.
- Removed the clean merged feature worktree normally after verifying its state. After the promoted-evidence audit, deleted every enumerated temporary screenshot, HTTPS proxy, Playwright-output, and evidence directory; the promoted 11-image set remains tracked.
- Did not execute remote `DROP DATABASE`. Isolated databases `pl_results_fresh_20260814_a`, `pl_results_sealed_20260814_b`, and `pl_results_sealed_20260814_c` are distinct from production, still exist, and have zero active connections. Their deletion is not locally recoverable and requires direct owner authorization; no Neon recovery operation was requested.
- Marked the product release complete. Evidence-PR merge, local-main synchronization to that merge, and removal of this closeout worktree remain repository-hygiene closeout tasks.

### August 13 roster release and repository closeout (complete)

- Audited the existing `roster/2026-08-13-player-update` worktree at `7d1996ca5e11dd279fd36cc33d4f7269befd613f`. Its tracked tree was initially clean, it was one direct commit ahead of live `main`, and it incorrectly tracked `origin/main`; no remote feature branch or pull request existed.
- The owner selected the August 13 handoff as release authority with complete internal handoff-to-application reconciliation and no independent official-club or Premier League roster verification.
- Selected the exact refreshed portrait set with 17 obsolete paths removed and four club-move paths renamed; no legacy compatibility copies were retained. The owner accepted the brief deploy-before-seed interval in which old database paths could use `PlayerMark`'s silhouette; the supported seed ended that window. Historical database player rows remain safe because the seed deactivates absent players rather than deleting them.
- Recorded the pre-release August 8 production baseline: 587 total/active players and 580 portrait paths. At that audit point the August 13 fixture was not merged, deployed, or seeded, so release identifiers were deliberately left pending.
- Verified the August 8 primary/worktree handoff payloads, copied the August 13 handoff byte-for-byte into the primary repository, and reconciled `PL Players!A1:K583` across all 582 rows and 11 columns against raw JSON, final JSON, and the tracked fixture. The final 582 source/public portraits all decode as unique 192×192 PNGs with byte parity and no silhouette match, placeholder, or duplicate group.
- Generated and visually inspected the 33-tile additions/restorations/replacements/moves contact sheet. After its report passed, removed exactly the approved `progress.json` and two stale mismatch reports from both August 13 copies. The retained copies are byte-identical at 613 files, 24,912,376 bytes, and full manifest SHA-256 `f625470979c156e233300ec42924eb858255d902a456af1283a089ae41169630`; the report SHA-256 is `d6652f98274a7087e2a53a098b8c649ff4e349a76419ee777935a8c771677046` and the contact-sheet SHA-256 is `326ce20cac6a8be3ba605c0859a43bba10f74f004e80a8e5d132915bf58eb3b1`.
- Hardened catalogue publication so all source, provenance, identity-delta, decode/hash, derived-field, and clean-clone fingerprint checks complete before a staged write with rollback. Pinned `sharp@0.35.3` directly for full image validation and added failure-before-mutation, traversal, wrong-identity, incomplete-handoff, provenance, and release-drift tests.
- Hardened the Markdown-to-HTML generator with NUL-safe Git-scoped discovery, private-path exclusion, symlink and unmanaged-collision refusal, current/legacy ownership markers, atomic writes, marker-owned orphan cleanup, deterministic source hashes/headings/link rewriting, exact CLI validation, and ten focused process tests. Regenerated all 14 managed peers.
- Hardened the isolated-database wrapper with canonical pooled/unpooled identity comparison independent of credentials, protocol/default-port and Neon host normalization, trailing-dot and ambiguous-host rejection, target-specific child attestation, and fail-closed Vitest/Playwright routing. Added seed idempotence/inactive-history, active-selector, historical-rendering, `PlayerMark` image-error, additions/removals/moves/restorations, wrapper-spoof, and production-smoke selector/image coverage.
- The first post-kickoff Playwright pass caught the test clock still recognizing the retired static isolation marker. Updated it to validate the shared target-specific attestation, added legacy-spoof and target-mismatch tests, and passed the corrected isolated browser run.
- Passed the uninterrupted final `CI=1 npm run check` against the attested isolated Neon database: 14-document parity, exact 582-player/portrait validation, formatting, ESLint, strict TypeScript, 205 unit/component tests with 11 guarded integration skips, all 11 integration tests, Webpack production build, five pre-kickoff browser journeys, and three post-kickoff browser journeys. The separate focused set passed 77 tests.
- Completed the initial read-only production recovery preflight without mutating participant data. It captured the 20-team, 587-total/active-player, zero-inactive, 580-path baseline but could not yet authenticate the configured restore window, so production seeding correctly remained stopped.
- Pushed the tested feature, passed every available GitHub and Vercel preview check, and merged [PR #15](https://github.com/vdoshi96/Pl-predictions/pull/15) on 2026-08-14 at 07:08:09 UTC as merge commit `cc4129dbd1513675bb44424cb4c667694c74d120`. Synchronized local `main` to that merge before production mutation.
- Verified Ready production deployment `dpl_2CAJEZ3cXpm7NU6K972sN31bVBkz` at [https://pl-predictions-835vjpg3z-vdoshi96s-projects.vercel.app](https://pl-predictions-835vjpg3z-vdoshi96s-projects.vercel.app) and read back [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app) as the same deployment ID.
- Proved the recovery stop gate in Neon project `young-leaf-03280061`: `main` is a root branch with parent `-`, the configured restore window is six hours, and the pre-seed capture at 2026-08-14 13:35:11.760453 UTC recorded WAL `0/45548C8` and the exact 20/587/587/0/580 baseline. A read-only time-travel query at 13:33:00 UTC returned that baseline. The Browse pane produced one transient signal-aborted error, but Query data succeeded; no restore was performed.
- Ran the supported production seed exactly once; it printed `Seeded 20 teams and 582 players for 2026/27 Premier League.` The post-seed capture at 2026-08-14 13:37:59.513827 UTC recorded WAL `0/4652E18`. Read-only verification found 20 teams; 599 total rows and 599 distinct IDs, external IDs, and slugs; 582 active; 17 inactive; 582 distinct active portrait paths; no null or missing club references; intact foreign-key and cross-season checks; zero inactive-player pick references; exact fixture parity; all expected inactive identities and moves; and zero active obsolete paths.
- Verified all 582 active portrait URLs as HTTP 200 with PNG content types and PNG signatures, and all 21 obsolete paths as HTTP 404. The final read-only smoke passed all five desktop/mobile Chromium and mobile WebKit projects, covering the representative addition, move, restored portrait, removal, Other-player silhouette, reflow, and browser/network/HTTP guards. Refreshed and visually inspected seven screenshots with manifest SHA-256 `6b5251c6fd96a70f5d32edc074101246303c76805deec1bf499ec382afee134d`; the exact deployment's bounded error, fatal, and HTTP 5xx queries returned no records.
- Merged evidence-only closeout [PR #16](https://github.com/vdoshi96/Pl-predictions/pull/16) on 2026-08-14 at 13:58:28 UTC as merge commit `98210965bfa6d586ef595c33038761e80f45b765`. Verified its documentation-only production deployment `dpl_9bUTkvP7x3Sr55GPMLmDyyuGVMv3` (`mr5jtzypm`) became Ready and returned HTTP 200. The stable alias intentionally remained on the previously verified roster deployment; no alias mutation ran. Fast-forwarded local `main` to the evidence merge and proved exact equality with `origin/main` before cleanup.
- Independently rehashed the retained primary August 8 handoff: full manifest `29d8dc2efda79ae5cde906214b91402008d89dbde71adca97e837aeab15e7ee3`, substantive non-`.DS_Store` parity manifest `e3c5d7c6ea5c75725fa4d3518333cd171e37d8a8cb407ecfa9fac0047bab3c5a`, and symlink path/target manifest `4e800f7c8103c59463469dfb492bdaaf1785a5f3e762a5427fc53596f540d888`. Its worktree duplicate matched all substantive files and symlinks; only two Finder `.DS_Store` files differed byte-for-byte.
- Independently rehashed the retained primary August 13 handoff at full manifest `e1ee3b985b8e26799d0ec06632ebf9a1250ac1fab6e5749be0b4574a67aec83d`; its worktree duplicate was byte-identical. The existing original-algorithm manifest `f625470979c156e233300ec42924eb858255d902a456af1283a089ae41169630` remains valid.
- Moved the verified worktree handoff duplicates recoverably to `/Users/vishal/.Trash/premier-league-players-2026-08-08-worktree-duplicate-2026-08-14` and `/Users/vishal/.Trash/premier-league-players-2026-08-13-worktree-duplicate-2026-08-14`, then removed the completed feature worktree normally and pruned worktree metadata.
- Proved ancestry before deleting remote `roster/2026-08-13-player-update`, `vishal/august-13-player-catalogue-closeout`, `agent/dranx-brand-refresh`, and `agent/dranx-release-closeout`. Deleted local `roster/2026-08-13-player-update` and `vishal/august-13-player-catalogue-closeout`. One primary worktree remains.
- Moved superseded QA and hash-identical output screenshot duplicates recoverably to `/Users/vishal/.Trash/Pl-Predictions-superseded-QA-2026-08-14`. Retained the newest seven canonical screenshots under `docs/assets/qa/` and the current output report/results.
- Intentionally retained the unrelated untracked `Premier League 2026-27 PNG Assets/` folder out of scope.

## 2026-08-13

### Owner handoff creation (local feature branch)

- The owner-provided handoff records an offline Transfermarkt 2026/27 first-team comparison with the August 8 snapshot: 12 additions, 17 removals, and four intra-league club changes, leaving 582 players.
- The handoff supplies portraits for the 12 additions and the seven former gaps, replaces ten prior fallbacks, and reports 582 portrait files with no catalogue gaps.
- The handoff includes identity, uniqueness, image, and upstream-comparison evidence. That evidence is intermediate owner-handoff material; the application release relies on a separate final internal reconciliation and does not claim independent official roster verification.
- The local feature commit pointed `players:generate` at `premier-league-players-2026-08-13/`, updated the tracked fixture and `public/player-faces/`, and retained Other player. It was not merged to `main` on August 13.

## 2026-08-08

### How-to, countdown, and complete test-entry iteration (complete)

- Traced the live Demo Alex and Demo Jordan spotlight cards to the hard-coded `LeaderboardDemo` component. A read-only production aggregate found zero prediction parents, table items, and category picks before the requested test entry, proving there were no malformed spotlight-only rows to delete.
- Removed the fixture component, its unconditional `/spotlight` render path, and demo-specific component/browser assertions. The real spotlight view now counts or ranks only complete persisted brackets.
- Added a compact calendar-flip days/hours/minutes/seconds timer beside “Submissions open”. Its starting duration is calculated from database time, its browser progression uses monotonic elapsed time, and it refreshes at zero without replacing the guarded PostgreSQL deadline check.
- Captured three 390-by-844 screens from the live public mobile flow and added them to `/rules` as a responsive three-step walkthrough. Numbered overlay pins have matching accessible callout text; the unsaved walkthrough draft created no production entry.
- Submitted the retained `Dranx Test Entry` through the supported public three-stage flow. Safe follow-up verification found one parent, 20 ordered table rows, and seven spotlight rows; `/leaderboard` shows the entry while `/spotlight` keeps its subjects private before reveal.
- Passed uninterrupted `CI=1 npm run check`: documentation/player parity, formatting, ESLint, strict TypeScript, 150 default tests with 10 guarded skips, all 10 isolated Neon integration cases, the Webpack production build, five pre-kickoff browser projects with 20 intentional skips, and three post-kickoff browser projects with two intentional skips. An earlier attempt stopped at Prettier before any lint or test phase and was corrected before the clean run.
- Merged GitHub [PR #13](https://github.com/vdoshi96/Pl-predictions/pull/13) into `main` at `a7785b9b16b64be825e06ef8f181ab5f98031524`. Verified Ready production deployment `dpl_2L4cprnsWShhesujY7sTLKffywhg` at [https://pl-predictions-9h7r762li-vdoshi96s-projects.vercel.app](https://pl-predictions-9h7r762li-vdoshi96s-projects.vercel.app), moved the stable alias to it, and read back the same deployment ID through the alias.
- Passed the final five-project read-only production smoke against the immutable deployment across desktop Chromium, 390-pixel mobile Chromium, exact 320/430-pixel reflow, and mobile WebKit. A dedicated second mobile pass captured the top-of-page countdown.
- Refreshed and visually inspected the newest seven production artifacts: countdown, prediction sorter, spotlight stage, final review, private spotlight state, rendered annotated how-to, and administrator login. Removed the stale Alex/Jordan demo screenshot from the retained QA set.
- A final read-only aggregate found one prediction parent named exactly `Dranx Test Entry`, 20 table items, seven spotlight picks, and no Alex/Jordan display-name rows. Exact-deployment error-level and HTTP 500 queries returned no records in the preceding 30 minutes.
- Regenerated every HTML peer, published the closeout through `main`, synchronized local and remote `main`, removed completed branch state, and retained only the primary worktree. The two owner handoff directories remain untouched and untracked.

### Research and provisioning

- Read the complete greenfield product brief and the owner's no-live-API/no-cron and mobile-first overrides.
- Verified official 2026/27 league membership and the 20 seeded club display names.
- Recorded FotMob's automated-extraction restriction and the fact that a consumer subscription does not grant automation or redistribution rights.
- Selected the source-neutral authenticated importer plus manual owner entry, with local monograms until crest redistribution rights are cleared.
- Verified Vercel CLI authentication, created and linked `vdoshi96s-projects/pl-predictions`, and provisioned Vercel Marketplace Neon resource `neon-coffee-queen` before database work.
- Generated server-only credentials, stored the owner login in macOS Keychain, configured Vercel environments without exposing values, pulled local environment names, applied the committed migration, and seeded exactly 20 teams.
- Initialized the local Git repository and created branch `agent/build-pl-predictions` after the bootstrap checkpoint.

### Implementation

- Built the mobile-first prediction sorter, review dialog, atomic submission flow, receipt-gated confirmation, fairness deadline/lock/reveal policy, pre-reveal roster, derived leaderboard, and entry comparison.
- Built the owner login/session, status dashboard, season settings, submission deletion, manual standings editor, import history, 38-game final-candidate check, explicit final confirmation, and undo.
- Added the canonical version-1 snapshot/failure schema, local import script, 64 KiB bearer-authenticated intake route, deterministic content hashes, stale/duplicate/final rejection, run history, and last-good preservation.
- Added compare-and-swap guards in both import activation and one-statement finalization so concurrent transitions cannot split the active and final snapshots.
- Added strict server validation, database constraints/indexes, receipt hashing, constant-time credential checks, signed HttpOnly sessions, same-origin admin mutations, honeypot, audit metadata, safe errors, security headers, and no-secret caching behavior.
- Added 20 independent local monogram assets, a responsive header/footer, unofficial rights disclaimer, safe-area mobile action, 56-pixel handle targets, keyboard announcements, focus states, and reduced-motion behavior.
- Added deterministic Markdown-to-HTML generation, repository context documents, focused unit/component/integration tests, a fail-closed isolated test-database wrapper, and reversible Playwright journeys across desktop, 320/390/430-pixel Chromium, and iPhone WebKit.
- Hardened prediction writes into one database-clock-guarded SQL statement; early reveal now closes submissions irreversibly, and hidden pre-reveal server payloads omit prediction identifiers and items.
- Added a five-minute database-time timestamp bound, season-level standings accepted-through watermark, historical-content reactivation, duplicate-watermark handling, and exact compare-and-swap final undo.

### Verification and deployment

- Passed formatting, ESLint with zero warnings, and strict TypeScript checks before documentation closeout.
- Passed 95 default-suite tests; the 9 database cases were skipped there by design and then all passed under `npm run test:integration` against isolated Neon database `pl_predictions_test`.
- Passed `npm run build:verify` with Next.js Webpack mode. The restricted local environment blocked an internal Turbopack helper port, while Vercel's default Turbopack production build later passed.
- Passed 5 browser journeys with 20 intentional project-routing skips across desktop, exact 320/390/430-pixel Chromium, and iPhone 13 WebKit. The run covered no-overflow layout, real mouse/touch/keyboard reorder, 56-pixel touch handles, review/submit, raw HTML/RSC hidden-entry privacy, pre-reveal leaderboard, admin manual standings, recalculation, comparison, exact-ID deletion, and reversible isolated-database cleanup.
- `npm audit --omit=dev --audit-level=high` found 0 vulnerabilities. The full audit found 4 moderate development-only legacy `esbuild` paths through `drizzle-kit`, no high/critical findings, and only a breaking forced downgrade; no force change was applied.
- Applied forward migration `0001_left_iron_fist.sql` to production before deploying code that reads its new watermark, then reran the idempotent 20-team seed.
- Received explicit owner approval and changed Vercel Authentication to `preview`. Anonymous production now returns 200 while the retained preview URL continues to return a 302 Vercel SSO redirect; preview protection was not disabled.
- Traced a Vercel edge 404 on an otherwise Ready production deployment to a null project framework preset. Set the preset to `nextjs`, deployed Ready production `dpl_JCfjcxvtVVfbsU55mmTppBK2FB9G` at [https://pl-predictions-65aqenfnq-vdoshi96s-projects.vercel.app](https://pl-predictions-65aqenfnq-vdoshi96s-projects.vercel.app), and mapped [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app) to it.
- Passed the final anonymous production smoke in all 5 projects across desktop Chromium, mobile Chromium at 390 pixels, exact 320/430-pixel reflow, and iPhone WebKit. Health, no-overflow, mouse/touch/keyboard reorder, and zero console, page, unexpected same-origin request, or HTTP response error assertions passed; only expected canceled speculative `_rsc` prefetches were excluded.
- Retained and visually inspected only the newest prediction, review, leaderboard, and administrator-login mobile screenshots under `docs/assets/qa/`.
- Exact-deleted four legacy `Reflow QA` predictions by UUID before the final smoke; none had a prediction-target audit row, and the follow-up query found zero residue.
- Passed the explicitly gated 1-case production write smoke: exact QA submit, receipt-only unrevealed access, hidden public roster details, and administrator deletion of the exact created prediction. Final queries found zero predictions, items, or prediction-target audits; the season remained open, unrevealed, and unlocked with null active/final pointers.
- Reviewed Vercel logs for the preceding hour and found zero error entries and zero HTTP 500 responses.

### Final closeout

- Refreshed final production evidence and labeled screenshot references in canonical Markdown.
- Regenerated every HTML peer and reran parity/format checks before publication.
- Published this final evidence update through the closeout branch, merged it into GitHub `main`, synchronized local and remote `main`, and removed completed branch/worktree state.

### Repository publication

- Published the tested feature history to [vdoshi96/Pl-predictions](https://github.com/vdoshi96/Pl-predictions), merged it into the canonical `main` release branch, synchronized local and remote `main`, and removed completed branch/worktree state.
- Published the final production evidence and mobile screenshots to the same canonical `main` release branch; local and remote release state are synchronized.

### Dranx branding iteration (complete)

- Selected **Dranx Prediction League** as the user-facing identity while retaining the repository, Vercel, Neon, URL, environment, and credential identifiers already used operationally.
- Defined a Premier-League-inspired palette anchored on official purple `#37003c` with cyan `#05f0ff`, green `#00ff87`, and pink `#ff2882` accents and an original Dranx mark.
- Confirmed that FotMob crest downloads and the official Premier League logo were not added because repository policy and the current official terms require authorization before copying or redistributing those marks.
- Retained the 20 local monograms and updated `TeamMark` with contain sizing and a labelled initials fallback for transparent authorized crest assets in a future permitted update.
- Completed canonical Markdown and deterministic HTML updates, ESLint, strict TypeScript, 97 default tests with 9 guarded skips, 9 isolated Neon integration tests, the Webpack production build, and 5 focused Playwright journeys with 20 routing skips. The aggregate `npm run check` passed before the final production-evidence refresh.
- Deployed Ready production `dpl_BPJUGKA7CJF7bhaUCTHjUx2cPEK7` at [https://pl-predictions-pizpoqt4u-vdoshi96s-projects.vercel.app](https://pl-predictions-pizpoqt4u-vdoshi96s-projects.vercel.app) with target `production`; Vercel built Next.js 16.3.0 with Turbopack and the stable public alias was repointed to it.
- Confirmed that the stable public alias returned 200 while the retained protected preview returned 302 to Vercel SSO.
- Passed the final read-only production smoke against the stable alias in all 5 projects across desktop Chromium, mobile Chromium at 390 pixels, exact 320/430-pixel reflow, and mobile WebKit. No browser, page, unexpected same-origin request, or HTTP errors occurred; the request hook excluded only cross-browser expected canceled `_rsc` navigational prefetches.
- Reviewed Vercel logs for the preceding hour and found zero error entries and zero HTTP 500 responses.
- Refreshed and visually inspected the prediction, review, leaderboard, and administrator-login production screenshots at 390 by 844 pixels.
- Kept the branding-only production verification read-only: no production write smoke ran and no production data changed. The prior release's exact-ID write and cleanup evidence remains intentional history.
- Merged GitHub [PR #1](https://github.com/vdoshi96/Pl-predictions/pull/1) successfully into canonical `main` at merge SHA `a0586831332db66158975928e98246eaf72118f1`. Its head was `2a0008e`, and its Vercel status was successful.
- Fast-forwarded local `main` to match `origin/main` at that merge before creating the final closeout branch, then removed the completed feature branch.
- Published the final closeout documentation through canonical `main`, synchronized local and remote `main`, removed the completed local temporary closeout branch after its merge, and retained one primary worktree. The final closeout merge SHA is intentionally not pinned in this durable end-state note.

### Gameweek 1 lock and champion leaderboard iteration (complete)

- Verified from the official Premier League opening-weekend announcement and complete fixture list that Arsenal v Coventry City is the first 2026/27 match at 20:00 BST on Friday 21 August 2026, exactly `2026-08-21T19:00:00.000Z`; recorded that all fixtures remain subject to change.
- Added migration `0002_breezy_king_cobra.sql` to persist and backfill the verified kickoff per season while retaining null as the automatic-deadline sentinel. A configured owner deadline may close earlier, while null or later values cannot bypass the persisted ceiling.
- Aligned public access/reveal decisions with PostgreSQL's wall clock. The atomic statement now acquires the season-row lock before sampling `clock_timestamp()`, so a write blocked across either deadline inserts neither parent nor items.
- Changed the preseason leaderboard to show every participant at 0 points with only the predicted champion's local mark/name. The prediction UUID and positions 2–20 remain absent from public HTML and RSC.
- Added post-kickoff champion status: 1st is “On track”; every other actual ordinal position is “Off track”. Scoring now requires kickoff, a meaningful table, and an accepted/re-observed timestamp at or after kickoff, so a stale preseason ordering cannot activate automatically.
- Added a production-disabled isolated clock seam and fresh-server requirement so tests remain deterministic after the real kickoff. The post-kickoff browser phase proves stale-table suppression, 100/96-point totals, ranks, champion actual positions, and scored-card reflow at desktop/320/430 pixels.
- Passed the final `CI=1 npm run check`: documentation parity, formatting, ESLint, strict TypeScript, 115 default tests with 10 guarded database skips, all 10 isolated Neon integration tests, the Webpack production build, and 8 Playwright journeys with 22 intentional routing skips across deterministic pre/post-kickoff phases.
- Applied `0002_breezy_king_cobra.sql` to production and verified opening kickoff `2026-08-21T19:00:00.000Z`, a null optional earlier deadline, unlocked submissions, and unrevealed predictions.
- Merged GitHub [PR #3](https://github.com/vdoshi96/Pl-predictions/pull/3) into canonical `main` at merge SHA `65841c8d458c1912c25a151c531b2d0538490fc5`; its tested head was `3191afa`, and its Vercel status passed.
- Deployed Ready production `dpl_4UEPWvwSECSwCHMUvLpacwmNjo1t` at [https://pl-predictions-428nljcvk-vdoshi96s-projects.vercel.app](https://pl-predictions-428nljcvk-vdoshi96s-projects.vercel.app) with target `production` and repointed the stable alias.
- Corrected the read-only production-smoke state detector after its first harness-only failure: Review is intentionally name-gated, so the test now distinguishes open/closed seasons through the name field and verifies Review enables after valid input.
- Passed the clean production-smoke rerun in all 5 projects across desktop Chromium, mobile Chromium at 390 pixels, exact 320/430-pixel reflow, and mobile WebKit, with zero browser, page, unexpected same-origin request, or HTTP errors.
- Reviewed Vercel runtime logs for the preceding 15 minutes and found zero error entries and zero HTTP 500 responses.
- Kept browser verification read-only; no production prediction or standings rows were written. Recaptured and visually inspected the newest prediction, review, leaderboard, and administrator-login mobile evidence.
- Regenerated and checked every HTML peer, published the evidence-only closeout through canonical `main`, synchronized local and remote `main`, removed completed branch/worktree state, and retained one primary worktree.

### Owner-provided club badge iteration (complete)

- Received a project-owner-supplied folder containing exactly one transparent 2048-by-2048 PNG badge for each of the 20 verified clubs plus eight out-of-scope Premier League logo/lion/ball/composite files.
- Copied only the 20 club badges into `public/team-marks/` under canonical slug filenames, changed all fixture paths from SVG to PNG, retained the original monograms as rollback-only first-release files, and excluded all league-brand extras.
- Kept the shared `TeamMark` contain sizing and labelled initials-on-error fallback, updated the user-facing rights wording, and added fixture coverage that requires a local PNG file for every canonical team path.
- Planned the production transition as deploy first, then idempotent seed, so existing Neon SVG paths remain valid until the PNG files are available.
- Passed the aggregate local `CI=1 npm run check`: documentation parity, formatting, ESLint, strict TypeScript, 115 default tests with 10 guarded skips, all 10 isolated Neon integration cases, the Webpack production build, and 8 deterministic browser journeys with 22 intentional project-routing skips.
- Merged GitHub [PR #5](https://github.com/vdoshi96/Pl-predictions/pull/5) into canonical `main` at `1c8a20bc0a689f39d5cb392753d3582aa616bdbc`, then verified Ready production deployment `dpl_2z72V3tGKDssEAggjqMEH4PQQsYN` at [https://pl-predictions-6hs8vokup-vdoshi96s-projects.vercel.app](https://pl-predictions-6hs8vokup-vdoshi96s-projects.vercel.app) and repointed the stable alias with explicit owner approval.
- Confirmed the stable alias served the home page and raw PNG files with 200 responses before running the explicitly approved idempotent production seed. The active season changed from 20 SVG/0 PNG paths to 0 SVG/20 PNG paths with no fixture mismatch; the non-path team metadata digest stayed `dc907bd0e26186a76d62210381ee593f44225f7c0557f8cede6d43c7dfae7910`, while prediction, standings-snapshot, and standings-item counts each stayed zero.
- Strengthened the production smoke for Safari-style lazy loading: every prediction and review-dialog club mark must complete with a nonzero natural width, and only cancelled Next Image candidates for those already-required local marks are excluded from generic request-failure reporting.
- Passed the final read-only production smoke in all 5 projects across desktop Chromium, mobile Chromium at 390 pixels, exact 320/430-pixel Chromium reflow, and mobile WebKit. Refreshed and visually inspected the newest prediction, review, leaderboard, and administrator-login screenshots; the sorter and review dialog display the new badges without horizontal clipping.
- Independently inspected the stable alias in a headed browser: all 20 marks decoded from PNG sources, no initials fallback appeared, all 20 optimized image requests returned 200, the console had zero messages, and desktop plus narrow viewports had no horizontal overflow.
- Reviewed the exact deployment's newest 500 production log records in the preceding 30-minute window: all 500 were HTTP 200, with zero warnings, errors, 4xx, or 5xx responses.
- Diagnosed the first final aggregate rerun's WebKit timeout as a five-second UI wait expiring while the standings save still completed. Removed the exact isolated snapshot, two import runs, and two audit rows left by that attempt/retry, restored the season pointer/watermark, expanded only that server-action wait to 20 seconds, and passed both a 13.3-second targeted WebKit rerun and the clean complete aggregate chain. Final test-database verification found zero snapshots, runs, audits, or `Mobile QA` predictions.
- Regenerated and checked every HTML peer, published the evidence-only closeout through canonical `main`, synchronized local and remote `main`, removed completed badge branch/worktree state, and retained only the primary worktree. The final closeout merge SHA is intentionally not pinned in this durable end-state note.

### Spotlight predictions and reviewed player-catalogue iteration (complete; owner controls pending)

- Expanded the participant journey from one table screen into an immutable three-stage flow: order all 20 clubs and enter a display name, choose seven spotlight predictions, then review the complete entry before one atomic submission.
- Added top scorer, top assister, most clean sheets, underdog team, overrated team, underdog player, and overrated player. The four player categories use keyboard-accessible searchable selectors plus Other-player text input; most clean sheets and both team-opinion categories select clubs through the existing crest catalogue.
- Normalized the owner-provided `premier-league-players-2026-08-08/` handoff into 587 reviewed players across all 20 clubs, imported 580 local portrait PNGs, and retained seven intentional silhouette fallbacks. Search covers first, last, and full names, while Other remains available for unavailable or newly added players.
- Kept the player snapshot source-neutral and local at runtime: it supplies selector identities and portraits only, never outcome rankings, and none of its acquisition scripts are executed by the application.
- Added production migrations `0003` through `0005` for players, category picks, strict nullable-subject constraints, and cross-season integrity triggers. Hardened the guarded insert so one prediction persists only with exactly 20 ordered club rows and seven category rows; authenticated deletion cascades through both child sets, records its audit, and releases the normalized display name for reuse.
- Added the visible leaderboard spotlight test run with real player names, real available portraits, club crests for club categories, and a deliberate silhouette example. Added the public rules page and kept actual unrevealed spotlight picks out of public HTML, RSC, and route payloads.
- Implemented derived category scoring primitives: occupied ranks currently map from 20 points for first through 1 point for twentieth; team underdog is `average predicted position - actual position`, team overrated is the inverse, and player-rating lists run descending for underdog and ascending for overrated. The 20-to-1 curve and resulting 240-point overall maximum remain an explicit working assumption awaiting owner confirmation; predicted table position 1 remains the champion with no separate bonus.
- Recorded that five result rankings still need reviewed source-neutral inputs: top scorer, top assister, most clean sheets, underdog player, and overrated player. Custom Other-player names also require reconciliation to the eventual outcome subjects; missing outcomes remain pending rather than receiving zero.
- Replaced the administrator login path with explicit username plus salted PBKDF2-SHA-256 verification while retaining a controlled legacy fallback only for migration. Added hash-generation support, malformed/sentinel-value failure tests, and kept the raw owner password out of source, logs, and documentation.
- Passed the tested feature revision's complete `CI=1 npm run check`: 147 default tests with 10 guarded skips, all 10 isolated Neon integration cases, the Webpack production build, five pre-kickoff browser journeys with 20 intentional routing skips, and three post-kickoff journeys with two intentional skips.
- Strengthened browser proof for the new assets and touch flow. The production smoke now permits one bounded touch retry only when the first gesture does not move the club and still requires a changed order. It excludes a cancelled optimized player-face request only while explicit UI assertions require real catalogue portraits to decode with nonzero dimensions and require the custom Other-player mark to render an SVG silhouette with no image.
- Merged GitHub [PR #7](https://github.com/vdoshi96/Pl-predictions/pull/7) into the working `main` branch at exact merge SHA `c976c1719c8c17f8169b9c8b86248765821b203a`. Verified Ready production deployment `dpl_5KBCAG4xhrZuapSqMg5LK4i6myGR` at [https://pl-predictions-7lhcv198i-vdoshi96s-projects.vercel.app](https://pl-predictions-7lhcv198i-vdoshi96s-projects.vercel.app), confirmed its exact SHA, and verified that [https://pl-predictions-2026.vercel.app](https://pl-predictions-2026.vercel.app) points to it. No GitHub default-branch setting is asserted.
- Inspected production before the transition and found zero predictions and zero prediction items, with the new player and category-pick tables not yet present. Applied migrations `0003` through `0005`, ran the idempotent seed, and verified 587 active players, 580 portrait paths, seven null portrait fallbacks, and both cross-season triggers.
- Corrected the two production-smoke harness boundaries, then passed all 5 read-only projects across desktop Chromium, 390-pixel mobile Chromium, exact 320/430-pixel Chromium reflow, and mobile WebKit. The run covered the three-stage preview, seven completed picks, explicit portrait/silhouette assertions, visible leaderboard demo, rules, administrator login, no overflow, and browser/network error guards.
- Hardened and passed the explicitly gated one-case production write smoke: production mode accepts only `PLAYWRIGHT_ADMIN_PASSWORD`, disables screenshot/trace/video artifacts, uses the list reporter, and retained only `.last-run.json`. The run submitted one unique complete entry, proved its receipt-only privacy, authenticated through the owner login, exact-deleted it, verified the deletion audit, and removed the QA audit during teardown. Final queries again found zero prediction parents, table items, spotlight picks, or deletion-audit residue.
- Refreshed and visually inspected the newest four 390-by-844 production screenshots: table entry, complete review with player imagery and Other fallback, the visible spotlight leaderboard demonstration, and username/password administrator sign-in.
- Confirmed the configured username and PBKDF2 hash through the production login, removed `ADMIN_SECRET` from production, preview, and development inventories, and merged hardened closeout [PR #8](https://github.com/vdoshi96/Pl-predictions/pull/8) at `a9d1c12c804ff34db48450ff2e57bded64710022`.
- Verified Ready post-removal deployment `dpl_HT2uPdP5iJPaWuYtNFEknroRmTqG` at [https://pl-predictions-dixkq8ma0-vdoshi96s-projects.vercel.app](https://pl-predictions-dixkq8ma0-vdoshi96s-projects.vercel.app). Its metadata contains `ADMIN_USERNAME` and `ADMIN_PASSWORD_HASH`, excludes `ADMIN_SECRET`, identifies exact SHA `a9d1c12c804ff34db48450ff2e57bded64710022`, and the stable alias points to it.
- Diagnosed a final smoke-only accessibility boundary: rank-two Haaland content begins inside a closed details element, while rank-one Jordan begins open. The harness now keeps both details open before requiring the real Haaland portrait to decode and the Alysson fallback to render as a silhouette. The corrected final run passed all 5 browser projects.
- Passed the hardened bounded write smoke against the same hash-only deployment. It causally matched the app-created UUID to the configured production database, authenticated without the legacy fallback, deleted the exact 20-plus-7 entry, and left zero prediction, table-item, category-pick, or deletion-audit residue while retaining no credential-bearing browser artifacts.
- Left the administrator-login WAF in its live log-only state for verification; the final metrics query recorded exactly four intended bounded-login `POST` requests. Prepared a per-IP rate limit of 10 requests per 60 seconds with HTTP 429 on excess requests, but did not publish it because owner publication remains required. The staged rule is not live.
- Regenerated and checked all HTML peers, published the final evidence through working `main`, synchronized local and remote `main`, removed completed branch state, and retained one primary worktree. The self-referential final evidence merge SHA is intentionally not pinned in this durable end-state note.

### Table-only leaderboard and spotlight accuracy iteration (complete)

- Applied the owner's scoring clarification: the main leaderboard now uses only table-prediction points and remains capped at 100. Spotlight picks never change that score.
- Added a separate `/spotlight` page with overall and seven category sorts. With `N` active nondeleted brackets, a selected subject at outcome rank `r` earns `max(0, N + 1 - r)` accuracy points. Pending outcomes are excluded, resolved zero-point outcomes remain available, and equal overall totals share a competition rank.
- Kept the five non-table-derived outcomes pending for a future owner-run Codex automation. The application still has no runtime football API, production scraper, or Vercel Cron.
- Published the approved Vercel WAF rule for `POST /admin/login`: 10 requests per 60 seconds for each IP, with HTTP 429 for excess requests. Read-back found active version 2 and no draft.
- Passed uninterrupted `CI=1 npm run check`: 148 default tests with 10 guarded skips, 10 isolated Neon integration cases, the Webpack production build, five pre-kickoff browser journeys with 20 intentional skips, and three post-kickoff journeys with two intentional skips.
- Merged [PR #10](https://github.com/vdoshi96/Pl-predictions/pull/10) into `main` at exact SHA `dc7b91ffd20200b79a51593c15b7dcb92cb493ab`. Verified Ready production deployment `dpl_3tyxwNSXGwTSuLctieAiJHYB2K9d` at [https://pl-predictions-6yyae7a73-vdoshi96s-projects.vercel.app](https://pl-predictions-6yyae7a73-vdoshi96s-projects.vercel.app), then pointed the stable alias to it and read back the exact mapping.
- Strengthened the read-only production smoke to require all seven available demo portraits plus Alysson's silhouette. Added a narrow Mobile WebKit guard only for its cancelled unauthenticated `/admin?_rsc` prefetch message and a 90-second project timeout for the full selector journey. The final clean run passed all five browser projects.
- Refreshed and visually inspected the newest 390-by-844 prediction, review, spotlight-accuracy, and administrator-login screenshots. Exact-deployment queries found no error logs or HTTP 500 records in the preceding 30 minutes.
- Kept this presentation-only release read-only in production. No prediction, standings, administrator, schema, seed, or other production-data write ran.
- Audited the GitHub default-branch blocker. The repository account has administrator access, but the active fine-grained token lacks repository Administration permission. Changing the default branch to `main` requires Administration read and write for this repository; branch deletion also requires Contents read and write.

### 2026-08-08: GitHub default-branch closeout (complete)

- Task: finish the repository-default and legacy-branch cleanup after the owner updated the fine-grained token permissions.
- Outcome: changed the GitHub default branch from `agent/build-pl-predictions` to `main`, then deleted the fully merged legacy remote branch.
- Files changed: current status and this durable log, plus their generated HTML peers.
- Verification: GitHub read-back reports default branch `main` and account role `ADMIN`; the legacy branch has no remote ref; local `origin/HEAD` resolves to `origin/main`; local `HEAD` and `origin/main` match at `8c7f65b8dc911b2ea3b4110699009dbf9fb3c948` before this documentation-only closeout.
- Next handoff: use `main` as the base for future pull requests. No GitHub access blocker remains for the current repository workflow.
